# NAMINTO.EX — Journal des décisions

Format ADR (Architecture Decision Record) léger. Chaque décision liste son contexte, son statut et ses conséquences. Alimenté à chaque prompt du protocole `Les 30 prompts de vibecoding ultra-directifs`.

## ADR-001 — Dépôt git dédié et séparé

- **Date** : 2026-08-25
- **Contexte** : Naminto.Ex est un projet distinct de Naminto Académie ; les deux ne doivent jamais partager de code, dépôt ou configuration.
- **Décision** : dépôt git initialisé directement dans `D:\NAMINTO.EX VIBECODING`.
- **Statut** : Adopté.

## ADR-002 — Frontend Next.js 16 (App Router) + TypeScript

- **Date** : 2026-08-25
- **Contexte** : besoin d'une app web responsive (mobile/tablette/desktop/grands écrans), FR/EN, clair/sombre, avec intégration naturelle à Supabase (auth SSR, route handlers) et exigence explicite de zéro erreur TypeScript (Definition of Done du Master Prompt).
- **Décision** : Next.js 16 + TypeScript strict, App Router, ESLint, structure `src/`.
- **Alternative écartée** : React + Vite (SPA pure) — moins adapté à l'auth SSR et au routing back-office/user-app séparé prévu au Prompt 03.
- **Statut** : Adopté.
- **Conséquence** : `AGENTS.md` généré par Next.js 16 signale des changements par rapport aux versions antérieures — consulter `node_modules/next/dist/docs/` avant d'écrire du code spécifique à Next.js.

## ADR-003 — Gestionnaire de paquets npm

- **Date** : 2026-08-25
- **Contexte** : choix par défaut du scaffold, aucune contrainte contraire exprimée.
- **Décision** : npm (`package-lock.json` commité).
- **Statut** : Adopté.

## ADR-004 — Supabase comme backend/données

- **Date** : 2026-08-25
- **Contexte** : l'utilisateur confirme qu'un projet Supabase existe déjà pour Naminto.Ex, séparé de celui de Naminto Académie.
- **Décision** : Supabase (Postgres + Auth + Storage) sera le backend de données.
- **Statut** : Adopté en principe — **bloquant** : identifiants (URL, clés) à fournir avant le Prompt 04 (Identity), qui en dépend directement.

## ADR-005 — Pas d'implémentation CSS figée au scaffold initial

- **Date** : 2026-08-25
- **Contexte** : le Prompt 02 doit construire un Design System centralisé piloté par tokens (couleurs, typographie, spacing…). Figer un framework CSS dès le Prompt 01 risquerait de contraindre ce choix.
- **Décision** : scaffold Next.js créé sans Tailwind ; le choix de l'implémentation (Tailwind piloté par tokens, CSS Modules + tokens, ou autre) est différé au Prompt 02.
- **Statut** : Tranché à l'ADR-006 (Prompt 02).

## ADR-006 — Tailwind CSS v4 piloté par tokens CSS + class-variance-authority

- **Date** : 2026-08-25
- **Contexte** : le Design System (Prompt 02) doit exposer des tokens centralisés (couleurs, typographie, spacing, radius, shadows) réutilisables par tous les composants, avec support natif clair/sombre.
- **Décision** : Tailwind CSS v4 (config CSS-first via `@theme`), avec les tokens sémantiques définis comme variables CSS dans `globals.css` (`:root` / `.dark`) puis exposés à Tailwind. Les variantes de composants (Button, Badge, Alert…) utilisent `class-variance-authority` + `clsx`/`tailwind-merge` (helper `cn`).
- **Statut** : Adopté.
- **Conséquence** : toute évolution de la charte graphique se fait uniquement dans `src/app/globals.css` — aucune couleur ne doit être codée en dur dans les composants.

## ADR-007 — next-themes pour la gestion clair/sombre

- **Date** : 2026-08-25
- **Contexte** : besoin d'un thème clair/sombre persistant, sans flash de contenu non stylé (FOUC) au chargement.
- **Décision** : `next-themes` (stratégie `class` sur `<html>`), encapsulé dans `src/design-system/theme/theme-provider.tsx`.
- **Statut** : Adopté.

## ADR-008 — Radix UI comme primitives accessibles (Modal, Dropdown, Tabs)

- **Date** : 2026-08-25
- **Contexte** : la section 67 de l'architecture générale exige l'accessibilité (navigation clavier, lecteurs d'écran). Recoder à la main la gestion du focus, des portails et du clavier pour une modale/menu/onglets est risqué pour une application financière.
- **Décision** : `@radix-ui/react-dialog`, `@radix-ui/react-dropdown-menu`, `@radix-ui/react-tabs`, `@radix-ui/react-label`, `@radix-ui/react-slot` comme primitives headless, stylées avec les tokens du Design System.
- **Statut** : Adopté.

## ADR-009 — lucide-react comme bibliothèque d'icônes

- **Date** : 2026-08-25
- **Contexte** : aucune charte d'icônes propriétaire n'existe. Construire un set d'icônes custom sans direction artistique serait prématuré.
- **Décision** : `lucide-react` (icônes SVG, tree-shakeable, cohérentes avec un style « trait fin » adapté à un produit fintech premium).
- **Statut** : Adopté — remplaçable sans impact structurel si une charte d'icônes officielle est fournie plus tard.

## ADR-010 — i18n FR/EN : dictionnaire léger au Prompt 02, routing différé au Prompt 03

- **Date** : 2026-08-25
- **Contexte** : le Prompt 02 exige que le Design System « supporte FR/EN ». Une solution de routing i18n complète (ex. `next-intl` avec segments de locale) est une décision structurante qui recoupe le routing de l'Application Shell (Prompt 03, séparation USER APP / BACK OFFICE).
- **Décision** : pour le Prompt 02, un `LocaleProvider` léger (contexte React + dictionnaires `fr`/`en` dans `src/design-system/i18n/`) suffit à démontrer et tester chaque composant dans les deux langues. Le choix définitif de la solution de routing i18n pour l'ensemble de l'application est différé au Prompt 03.
- **Statut** : Adopté pour le Design System — **TODO_DECISION** pour l'i18n au niveau applicatif (voir tableau ci-dessous).

## ADR-011 — Application Shell : séparation par arborescence de routes, i18n applicatif sans préfixe d'URL

- **Date** : 2026-08-25
- **Contexte** : le Prompt 03 exige que USER APP et BACK OFFICE soient « structurellement séparés », et fournit une liste de routes principales à créer.
- **Décision** :
  - Séparation via l'arborescence Next.js : l'espace utilisateur vit dans le groupe de routes `src/app/(user)/` (racine `/`, ex. `/send`, `/history`…) ; le Back Office vit sous le segment réel `src/app/admin/` (préfixe `/admin/...`), chacun avec son propre `layout.tsx`.
  - Un seul composant `Shell` (`src/shell/shell.tsx`), paramétré par `variant: "user" | "admin"`, compose Header + Sidebar + navigation mobile pour les deux espaces — évite de dupliquer un composant pour deux usages quasi identiques (cf. règle Prompt 02 « ne duplique aucun composant »).
  - i18n : on conserve le `LocaleProvider` léger du Prompt 02, maintenant utilisé pour toute l'application (persistance via `localStorage`, lu de façon hydration-safe avec `useSyncExternalStore`). Pas de préfixe de locale dans l'URL (`/fr/...`, `/en/...`) : aucune spécification n'exige de SEO multilingue ni d'URLs localisées à ce stade, et une solution de routing complète (ex. `next-intl`) resterait à réévaluer si ce besoin apparaît.
  - Toutes les routes principales (10 côté utilisateur, 17 côté Back Office) sont créées avec un état « Bientôt disponible » via un composant `ComingSoonPage` partagé, conformément au Prompt 03. Le tableau de bord utilisateur (`/`) est lui aussi à l'état « Bientôt disponible » — aucune donnée financière n'est simulée, conformément à « Ne mets aucune logique financière dans le shell ».
- **Statut** : Adopté. Referme le TODO_DECISION i18n applicatif ouvert par l'ADR-010.
- **Conséquence** : passer les tableaux de navigation (icônes Lucide, composants fonctions) d'un Server Component vers `Shell` (Client Component) échoue à la sérialisation RSC — `Shell` importe donc directement `userNavItems`/`adminNavItems` en interne plutôt que de les recevoir en props depuis les layouts.

## ADR-012 — Sheet (panneau latéral) ajouté au Design System pour la navigation mobile

- **Date** : 2026-08-25
- **Contexte** : le Prompt 03 exige une navigation mobile dédiée. La barre d'onglets basse ne peut afficher que 4 éléments primaires ; le reste du menu doit être accessible via un panneau.
- **Décision** : ajout de `Sheet`/`SheetContent` (`src/design-system/components/sheet.tsx`) au Design System, construit sur `@radix-ui/react-dialog` (même primitive que `Modal`, avec positionnement latéral). Réutilisé par `MobileNav` pour afficher la liste complète des liens.
- **Statut** : Adopté.

## ADR-004bis — Identifiants Supabase reçus et projet connecté

- **Date** : 2026-08-25
- **Contexte** : suite de l'ADR-004, bloquante avant le Prompt 04.
- **Décision** : URL, clé publique (`sb_publishable_...`) et clé secrète (`sb_secret_...`) du projet Supabase de Naminto.Ex reçues et renseignées dans `.env.local` (non commité). Connexion Postgres directe testée : le sous-domaine `db.<ref>.supabase.co` ne se résout pas depuis cet environnement de développement (limitation réseau locale, probablement IPv6 requis) — la **Session Pooler** (`aws-1-eu-west-1.pooler.supabase.com`, IPv4) fonctionne et est utilisée pour les migrations exécutées depuis l'environnement de dev.
- **Statut** : Adopté.
- **Conséquence** : si une exécution future de migration échoue avec `ENOTFOUND db.*.supabase.co`, utiliser la chaîne Session Pooler plutôt que la connexion directe.

## ADR-013 — Authentification : Supabase Auth (email + mot de passe) + tables custom pour PIN/appareils/audit

- **Date** : 2026-08-25
- **Contexte** : Prompt 04 (Identity). Éviter de réinventer une couche d'authentification pour un produit financier (cf. Prompt 30 vibecoding : « ne code aucune authentification fictive »).
- **Décision** :
  - Supabase Auth gère l'inscription, la connexion, le mot de passe et les sessions (email + mot de passe pour l'instant — voir ADR-014 pour le téléphone/SMS).
  - Le PIN Naminto.Ex, les appareils et l'historique de sécurité vivent dans des tables Postgres custom (`supabase/migrations/0001_identity.sql` : `identity_profiles`, `pin_credentials`, `devices`, `security_events`), reliées à `auth.users.id`.
  - `pin_credentials` n'a **aucune policy RLS de lecture**, y compris pour le titulaire : le hash du PIN ne quitte jamais la base, la vérification passe toujours par un Route Handler / Server Action utilisant le client `service_role` (`src/lib/supabase/admin.ts`, protégé par le paquet `server-only`).
  - `security_events` est en lecture seule pour l'utilisateur (RLS `select` uniquement) — toute écriture passe par `service_role` (`src/domains/identity/security-events.ts`), pour qu'aucun utilisateur ne puisse forger son propre historique.
  - PIN : 6 chiffres, haché avec `bcryptjs` (12 rounds), verrouillage après 5 échecs pendant 15 minutes (`src/domains/identity/pin.ts`).
  - Identifiant d'appareil : cookie httpOnly opaque (`nx_device_id`, UUID aléatoire) plutôt qu'un fingerprinting technique (canvas, user-agent complet) — conforme au principe de privacy by design (section 65 de l'architecture).
- **Statut** : Adopté.

## ADR-014 — Téléphone/SMS : différé, e-mail comme identifiant principal pour l'instant

- **Date** : 2026-08-25
- **Contexte** : le parcours d'inscription de référence prévoit une confirmation par SMS. Cela nécessite un fournisseur SMS payant (Twilio, MessageBird…) configuré dans Supabase, pas encore en place.
- **Décision** : l'inscription utilise e-mail + mot de passe (confirmation par e-mail via le mailer intégré de Supabase, fonctionnel sans configuration supplémentaire). Le numéro de téléphone reste un champ du profil (`identity_profiles.phone_number`), non vérifié (`phone_verified = false`), collecté mais inactif.
- **Statut** : Adopté temporairement — **TODO_DECISION** : activer le flux téléphone + OTP SMS une fois un fournisseur configuré (voir tableau ci-dessous).
- **Conséquence** : `public.identity_profiles.phone_verified` reste à `false` pour tous les comptes tant que ce point n'est pas traité ; ne pas construire de logique dépendant d'un numéro vérifié avant cette activation.

## ADR-015 — Confirmation e-mail et réinitialisation de mot de passe via `/auth/confirm`

- **Date** : 2026-08-25
- **Contexte** : Supabase Auth exige par défaut la confirmation de l'adresse e-mail avant qu'une session soit créée (vérifié empiriquement : `signUp()` renvoie `session: null` tant que le lien n'est pas cliqué).
- **Décision** : Route Handler unique `src/app/auth/confirm/route.ts` gérant `token_hash` + `type` (`supabase.auth.verifyOtp`), utilisé à la fois pour la confirmation d'inscription et la réinitialisation de mot de passe, avec un paramètre `next` pour la redirection post-vérification. C'est le pattern actuellement documenté par Supabase pour Next.js App Router.
- **Statut** : Adopté.
- **⚠️ Dépendance externe non vérifiable depuis le code** : ce mécanisme suppose que le modèle d'e-mail Supabase (Dashboard > Authentication > Email Templates) pointe vers `{{ .SiteURL }}/auth/confirm?token_hash={{ .TokenHash }}&type={{ .Type }}&next=...`. Si les modèles par défaut de ce projet utilisent encore l'ancien format (`{{ .ConfirmationURL }}` pointant vers la page hébergée par Supabase), la confirmation fonctionnera probablement quand même (Supabase redirige ensuite vers `redirectTo`), mais à vérifier lors du premier test réel avec un vrai fournisseur d'e-mail. Non testable en profondeur depuis cet environnement (voir Definition of Done ci-dessous).

## ADR-016 — `/admin` protégé par authentification seule, RBAC différé au Prompt 23

- **Date** : 2026-08-25
- **Contexte** : le Prompt 04 ne couvre que Identity, pas les rôles (Prompt 23). Le Back Office ne doit pourtant pas rester totalement ouvert.
- **Décision** : `src/proxy.ts` (ex-`middleware.ts`, renommé selon la convention Next.js 16 — voir Conséquence) exige une session valide pour accéder à `/admin/*`, exactement comme pour l'espace utilisateur. **Aucune vérification de rôle n'existe encore** : n'importe quel compte Naminto.Ex authentifié peut actuellement accéder au Back Office.
- **Statut** : Adopté comme mesure temporaire, **résolu au Prompt 23** (ADR-051) : `/admin` est désormais restreint par permission, pas seulement par authentification. `src/proxy.ts` reste la première ligne de défense (session valide) ; le contrôle par rôle vit dans `src/domains/rbac/`.
- **Conséquence** : `src/middleware.ts` renommé en `src/proxy.ts` (export `proxy` au lieu de `middleware`) — Next.js 16 a déprécié la convention `middleware`. Migration effectuée via lecture de `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/proxy.md`.

## ADR-017 — User étend identity_profiles plutôt qu'une table séparée

- **Date** : 2026-08-25
- **Contexte** : Prompt 05 (User Profile + KYC Foundation). La documentation d'architecture (section 68) présente Identity et User comme deux domaines distincts, et la spécification USER envisageait initialement une table `UserProfile` séparée.
- **Décision** : `identity_profiles` (créée au Prompt 04) est étendue par `supabase/migrations/0002_user_profile.sql` (`kyc_status`, `preferred_currency`, `notifications_enabled`, `sound_enabled`) plutôt que de créer une seconde table faisant doublon. Identity et User restent deux bounded contexts au niveau du **code** (`src/domains/identity/` vs `src/domains/user/`, requêtes et Server Actions séparées), mais partagent la même table racine — conforme à la règle du Master Prompt « ne crée jamais un deuxième système qui fait la même chose » et à la phrase de l'architecture générale : « L'identité Naminto.Ex constitue la racine de toutes les fonctionnalités personnelles ».
- **Statut** : Adopté.

## ADR-018 — KYC : colonnes protégées par trigger, jamais auto-attribuables

- **Date** : 2026-08-25
- **Contexte** : le Prompt 05 exige de préparer le modèle KYC sans prétendre à une vérification réelle, et d'ajouter permissions/audit. La policy RLS `identity_profiles_update_own` (Prompt 04) autorise la mise à jour de toute la ligne par son titulaire — un risque si `kyc_status` restait dans son périmètre.
- **Décision** : trigger `protect_privileged_identity_columns` (fonction `SECURITY INVOKER`, vérifie `auth.role()`) rejette toute tentative de modification de `kyc_status`, `status` ou `phone_verified` par un rôle autre que `service_role`, quelle que soit la policy RLS. Le badge KYC affiché dans `/settings` est donc purement informatif — aucune action de vérification n'existe encore côté produit (fournisseur externe à intégrer plus tard, hors périmètre de ce prompt).
- **Statut** : Adopté.
- **Vérifié empiriquement** : appel `PATCH` direct à l'API REST Supabase (`Authorization: Bearer <token utilisateur>`) tentant de passer `kyc_status` à `verified` → rejeté avec l'erreur du trigger (HTTP 400, `P0001`).

## ADR-019 — Préférences de langue/devise stockées mais non branchées sur l'UI live

- **Date** : 2026-08-25
- **Contexte** : le Prompt 05 demande que le profil « puisse contenir » langue et devise préférées. Le `LocaleProvider` (Prompt 02/03) pilote déjà la langue de l'interface via `localStorage`, indépendamment de tout compte.
- **Décision** : `identity_profiles.preferred_language` / `preferred_currency` sont des champs de profil modifiables depuis `/settings`, persistés en base, mais **ne pilotent pas** automatiquement le `LocaleProvider` ni un quelconque routage de devise — ce sont des préférences déclaratives pour l'instant (utile plus tard pour les notifications/SMS localisés, le Prompt 29 multi-devises). Éviter de re-architecturer le système i18n déjà fonctionnel pour un besoin non explicitement demandé.
- **Statut** : Adopté. Devise limitée à `XOF` (le sélecteur n'a qu'une option) — multi-devises différé au Prompt 29.

## ADR-020 — Accounts : liaison simulée en attendant le Provider Gateway (Prompt 07)

- **Date** : 2026-08-25
- **Contexte** : le Prompt 06 (Linked Accounts) précède le Prompt 07 (Provider Gateway) dans le protocole — aucun adapter fournisseur réel n'existe encore. Le Master Prompt interdit explicitement toute fausse intégration financière présentée comme réelle (« Zero fausse intégration financière »).
- **Décision** :
  - `linked_accounts` (`supabase/migrations/0003_accounts.sql`) stocke provider, référence externe, statut, capabilities (statiques pour l'instant), consent_status et timestamps — le modèle de données complet attendu par le Prompt 06.
  - Le flux de liaison affiche un avertissement explicite (« Démonstration — aucune connexion réelle ») et un écran de consentement détaillant ce que Naminto.Ex pourra/ne pourra jamais faire, conformément à la section 11 de l'architecture générale.
  - Aucun solde n'est simulé ou inventé : chaque carte affiche « Solde indisponible — fournisseur non connecté » plutôt qu'un chiffre fictif.
  - Aucun PIN ni identifiant fournisseur sensible n'est demandé — seule une référence (ex. numéro de téléphone) est saisie, toujours masquée à l'affichage (`maskExternalReference`, derniers 4 chiffres uniquement).
- **Statut** : Adopté. À reconnecter au vrai Provider Gateway lors du Prompt 07 (le statut `active` deviendra alors le résultat d'un appel réel à l'adapter, pas une valeur posée directement par le client).

## ADR-021 — Reconnexion : index unique partiel plutôt que suppression physique

- **Date** : 2026-08-25
- **Contexte** : le Prompt 06 exige explicitement la « reconnexion » d'un compte précédemment délié. La déliaison est un soft-delete (`status = 'unlinked'`), et la contrainte unique initiale (`user_id, provider, external_reference`) bloquait toute nouvelle liaison avec la même référence.
- **Décision** : remplacement par un index unique **partiel** (`where status <> 'unlinked'`, `supabase/migrations/0004_accounts_reconnect.sql`) — une seule ligne active par (utilisateur, fournisseur, référence), mais l'historique délié n'empêche pas une reconnexion. `linkAccountAction` détecte une ligne déliée existante et la réactive (même `id`, `linked_at` rafraîchi) au lieu d'en créer une seconde, journalisée comme `account_reconnected` (distinct de `account_linked`).
- **Statut** : Adopté.
- **Trouvé et corrigé pendant la vérification** : la première implémentation (contrainte unique simple) aurait renvoyé à tort « déjà lié » lors d'une tentative de reconnexion légitime — repéré en testant réellement le scénario contre la base, pas seulement en relisant le code.

## ADR-022 — Provider Gateway : un adapter générique paramétré plutôt que 5 classes dupliquées

- **Date** : 2026-08-25
- **Contexte** : le Prompt 07 demande explicitement 5 adapters nommés (`OrangeSandbox`, `MTNSandbox`, `MoovSandbox`, `WaveSandbox`, `CardSandbox`), tous implémentant la même interface `ProviderAdapter`. Dupliquer la logique de simulation (soldes, idempotence, statuts) dans 5 fichiers quasi identiques violerait la règle « ne duplique aucun composant/système ».
- **Décision** : une fabrique unique `createSandboxAdapter(config)` (`src/domains/providers/sandbox/create-sandbox-adapter.ts`) porte toute la logique de simulation ; les 5 fichiers nommés (`orange.ts`, `mtn.ts`, `moov.ts`, `wave.ts`, `card.ts`) ne font qu'appeler cette fabrique avec leur configuration propre (capacités, support du refund). Le `Provider Registry` (`src/domains/providers/registry.ts`) les enregistre par un `Map<Provider, ProviderAdapter>`. Ajouter un 6ᵉ fournisseur SANDBOX ne nécessite qu'un nouveau fichier de config + un appel `registerAdapter()` — zéro modification du cœur financier, conformément à l'exigence du Prompt 07.
- **Statut** : Adopté.
- **Vérifié empiriquement** (script autonome, hors Next.js) : idempotence (rejeu de la même clé → même transaction, aucun double débit), échec pour solde insuffisant, refus d'annuler une transaction déjà confirmée, refund respectant `supportsRefund` par fournisseur, health check, parsing de webhook.

## ADR-023 — Accounts (Prompt 06) rebranché sur le Provider Gateway

- **Date** : 2026-08-25
- **Contexte** : le domaine Accounts (Prompt 06) avait été construit avant le Provider Gateway et créait les lignes `linked_accounts` directement, avec des capacités statiques et un message « Solde indisponible ».
- **Décision** : `linkAccountAction` appelle désormais `getProviderAdapter(provider).linkAccount(...)` (capacités dynamiques, retournées par l'adapter) et la page `/accounts` appelle `adapter.getBalance(...)` pour chaque compte actif. Le solde s'affiche clairement étiqueté « Solde (SANDBOX) » — jamais présenté comme réel, conformément au Master Prompt. `src/domains/accounts/providers.ts` ne conserve que les métadonnées visuelles (libellé, couleur) ; les capacités ne vivent plus que côté Provider Gateway.
- **Statut** : Adopté. Referme partiellement le TODO_DECISION « capabilities statiques » de l'ADR-020 (deviennent dynamiques ; resteront simulées tant que le mode SANDBOX n'est pas remplacé par REAL).
- **Testé de bout en bout** : liaison MTN via l'UI → solde SANDBOX 250 000 XOF affiché immédiatement (valeur de départ par défaut de la fabrique sandbox).

## ADR-024 — Webhook générique public, persistance différée

- **Date** : 2026-08-25
- **Contexte** : le Prompt 07 exige que chaque adapter expose une capacité webhook. Aucun fournisseur réel n'appelle jamais une route SANDBOX, mais le contrat doit exister et être exerçable.
- **Décision** : route générique `POST /api/webhooks/[provider]` (`src/app/api/webhooks/[provider]/route.ts`), ajoutée aux chemins publics de `src/proxy.ts` (un fournisseur externe n'a jamais de session Naminto.Ex). Elle appelle `adapter.verifyAndParseWebhook()` et journalise l'événement — **aucune persistance en base pour l'instant** : le domaine Transaction (Prompt 08) n'existe pas encore pour rattacher un événement webhook à une opération réelle, et la vérification de signature réelle, l'idempotence et le rejeu contrôlé sont le périmètre explicite du Prompt 25 (Webhooks).
- **Statut** : Adopté.
- **Vérifié empiriquement** : appel réel à la route (200, événement journalisé côté serveur) et rejet d'un fournisseur inconnu (404).

## ADR-025 — Vitest comme framework de tests

- **Date** : 2026-08-25
- **Contexte** : le Prompt 08 exige explicitement des « tests de transition » pour la State Machine — ce TODO_DECISION technique ne pouvait plus rester ouvert.
- **Décision** : Vitest (`vitest.config.mts`, `npm run test` / `test:watch`). Choisi pour son intégration native avec l'écosystème Vite/TypeScript, sa rapidité, et l'absence de configuration lourde comparé à Jest sur un projet Next.js App Router.
- **Statut** : Adopté. Referme le TODO_DECISION « Framework de tests » ouvert depuis le Prompt 01.
- **Conséquence** : le paquet `server-only` lève toujours une erreur hors du pipeline de build Next.js (comportement voulu, pas un bug — Next le substitue par un no-op dans ses bundles serveur). `vitest.config.mts` l'alias vers `test/stubs/server-only.ts` pour permettre de tester le code serveur en dehors de Next.

## ADR-026 — Transaction : écriture exclusivement service_role, aucune policy RLS d'écriture

- **Date** : 2026-08-25
- **Contexte** : le Prompt 08 est explicite — « Ne crée aucune possibilité de modifier librement le statut depuis le frontend ».
- **Décision** : `transactions` et `transaction_status_events` (`supabase/migrations/0005_transactions.sql`) n'ont **aucune policy INSERT/UPDATE/DELETE** — seule la lecture (par l'expéditeur ou le destinataire) est autorisée côté client. Toute écriture passe par `src/domains/payments/transactions.ts` (`service_role`), qui est le seul point d'entrée légitime (`createTransaction`, `transitionTransaction`). Il n'existe volontairement aucune Server Action générique « changer le statut » exposée à un formulaire.
- **Défense en profondeur** : un trigger Postgres (`check_transaction_status_transition`) rejoue la même validation de la State Machine côté base — si le TypeScript avait un bug, la base refuserait quand même la transition. Le trigger doit être maintenu en miroir exact de `src/domains/payments/transaction-status.ts`.
- **Statut** : Adopté.
- **Vérifié empiriquement** (tests d'intégration Vitest, `transactions.test.ts`) : idempotence par clé (aucune transaction dupliquée), transition valide journalisée dans `transaction_status_events`, transition invalide rejetée côté application (`InvalidTransactionTransitionError`) **et** côté base (contournement délibéré du service pour prouver que le trigger seul suffit à protéger l'intégrité).

## ADR-027 — État `disputed` volontairement terminal

- **Date** : 2026-08-25
- **Contexte** : la State Machine documentée (`PAYMENTS — Spécification Markdown.docx`, `docs/ARCHITECTURE.md`) définit `Réglée → Inversée | Remboursée | Contestée` mais ne précise aucune transition de sortie pour l'état contesté.
- **Décision** : `disputed` n'a aucune transition sortante dans `ALLOWED_TRANSITIONS`, plutôt que d'inventer un chemin de résolution plausible.
- **Statut** : Adopté — TODO_DECISION ci-dessous pour le workflow de résolution des litiges.

## ADR-028 — Payment Orchestrator : étapes STUB indépendantes plutôt que reportées

- **Date** : 2026-08-25
- **Contexte** : le Prompt 09 précède les moteurs qu'il orchestre (Fee: Prompt 10, Limits: Prompt 11, Ledger: Prompt 12, Risk: Prompt 17, Fraud: Prompt 18, Compliance: Prompt 19, Notification: Prompt 20, Reconciliation: Prompt 24). Construire l'orchestrateur sans ces moteurs impose un choix : attendre qu'ils existent, ou les représenter par des étapes STUB déjà présentes dans le pipeline.
- **Décision** : chaque étape vit dans son propre module (`src/domains/payments/orchestrator-steps/`), avec la signature qu'elle aura une fois réelle. Les étapes déjà couvertes par une règle **documentée** sont implémentées réellement dès maintenant (Compliance : seuil KYC 200 000 FCFA ; Fee : taux flat 3,5 %) ; celles qui n'ont aucune règle documentée restent des STUB qui ne rejettent jamais (Risk, Limits) ou ne persistent rien (Ledger, Notification, Reconciliation). Brancher le moteur réel d'une étape ne nécessite de modifier que son propre module, jamais `orchestrator.ts`.
- **Statut** : Adopté.

## ADR-029 — Fee calculé avant la création de la transaction (déviation mineure du diagramme)

- **Date** : 2026-08-25
- **Contexte** : le diagramme du Prompt 09 place « Fee » après Risk/Compliance/Limits et « Transaction » après Provider Gateway. Mais le domaine Transaction (Prompt 08) exige un enregistrement dès le début pour le suivi d'état et l'idempotence, et `total` (montant + frais) est une colonne `not null` dès la création.
- **Décision** : le calcul du frais (pur, ne dépendant que de `amount`/`currency` — jamais du résultat de Risk/Compliance/Limits) est avancé avant `createTransaction`, pour que `fee`/`total` soient corrects dès la création plutôt que mis à jour a posteriori. Le reste de l'ordre (Risk → Compliance → Limits → Routing → Provider Gateway) suit exactement le diagramme.
- **Statut** : Adopté.
- **Trouvé et corrigé pendant la vérification** : la première version calculait le frais après la création de la transaction sans jamais le persister — `transaction.fee` valait 0 au lieu de 175 pour un envoi de 5 000 FCFA. Repéré par un test d'intégration réel, pas une relecture de code.

## ADR-030 — `isInFlight` distinct de `isTerminalStatus` pour le court-circuit de rejeu

- **Date** : 2026-08-25
- **Contexte** : « retries sûrs » exige qu'un rejeu de la même idempotencyKey après un paiement déjà réglé ne réexécute aucune étape à effet de bord.
- **Décision** : `isTerminalStatus` (Prompt 08, `ALLOWED_TRANSITIONS[status].length === 0`) renvoie **false** pour `settled`, puisque `settled` a des transitions sortantes exceptionnelles (`reversed`/`refunded`/`disputed`). Un nouveau helper `isInFlight` (`src/domains/payments/transaction-status.ts`) distingue les statuts « en cours » (`created` → `provider_confirmed`) des statuts « aboutis » (`settled` inclus, plus tous les échecs terminaux). Le Payment Orchestrator court-circuite dès que `!isInFlight(transaction.status)`.
- **Statut** : Adopté.
- **Trouvé et corrigé pendant la vérification** : la première version utilisait `isTerminalStatus`, qui ne court-circuitait jamais pour une transaction déjà réglée — le rejeu tentait de retransitionner `settled → validating` et échouait avec `InvalidTransactionTransitionError`. Repéré par le test d'intégration « retries sûrs », pas une relecture de code.

## ADR-031 — Correction Provider Gateway : `receive` créditait par erreur comme `transfer`

- **Date** : 2026-08-25
- **Contexte** : la fabrique SANDBOX du Prompt 07 aliasait `receive` directement sur la même fonction que `transfer`, qui **débite** toujours le compte lié. Or `receive` doit représenter l'argent qui **entre** dans le compte lié depuis Naminto.Ex (ex. destination = compte lié), donc **créditer**. Cette confusion n'avait aucune conséquence visible avant le Prompt 09 : rien n'appelait encore `receive()`.
- **Décision** : `create-sandbox-adapter.ts` a désormais deux fonctions distinctes — `executeTransfer` (débite) et `executeReceive` (crédite), chacune avec sa propre idempotence.
- **Statut** : Corrigé.
- **Trouvé pendant la vérification** : en écrivant le Payment Orchestrator (Prompt 09) et en réfléchissant à quelle méthode appeler selon que le compte lié est source ou destination — pas détecté au Prompt 07 faute d'appelant réel à l'époque. Une suite de tests Vitest dédiée (`create-sandbox-adapter.test.ts`, absente au Prompt 07) couvre maintenant explicitement les deux directions.

## ADR-032 — `transitionTransaction` accepte des champs additionnels à persister

- **Date** : 2026-08-25
- **Contexte** : `provider_transaction_id` était bien renvoyé dans l'objet transaction retourné par l'orchestrateur, mais jamais réellement écrit en base — la fonction `transitionTransaction` (Prompt 08) ne touchait que la colonne `status`.
- **Décision** : `transitionTransaction(id, to, reason?, extra?)` accepte un 4ᵉ paramètre optionnel (`{ providerTransactionId }` pour l'instant) fusionné dans la même requête `update`, pour que la transition de statut et l'enregistrement du résultat fournisseur restent atomiques.
- **Statut** : Corrigé.
- **Trouvé et corrigé pendant la vérification** : un test relisant `provider_transaction_id` **depuis la base** (pas seulement la valeur renvoyée en mémoire par l'orchestrateur) aurait échoué sans ce correctif — ajouté spécifiquement après avoir remarqué que rien n'appelait jamais d'`update` avec ce champ.

## ADR-033 — Fee Engine : Routing avancé avant la création de la transaction

- **Date** : 2026-08-25
- **Contexte** : le Prompt 10 exige explicitement que `provider` soit une dimension sur laquelle une règle de frais peut se spécialiser. Or `provider` n'était connu qu'après Routing, que le Prompt 09 plaçait après le calcul du frais dans son implémentation initiale.
- **Décision** : `routeRequest` est désormais résolu avant `calculateFee` et avant `createTransaction` (voir `orchestrator.ts`). Routing reste une lecture pure sans effet de bord — l'avancer ne change aucun résultat, et permet au Fee Engine de recevoir `provider` dès le premier calcul. Une erreur de routing (compte lié introuvable, non actif) est donc maintenant classifiée `VALIDATION_ERROR` et survient avant toute création de transaction, plutôt que `PROVIDER_ERROR` en cours de traitement.
- **Statut** : Adopté. Complète l'ADR-029 (Prompt 09) sur le calcul anticipé du frais.

## ADR-034 — Fee Engine entièrement piloté par la configuration (`fee_rules`)

- **Date** : 2026-08-25
- **Contexte** : Prompt 10 — « Ne code aucune règle tarifaire dans les composants UI » et « Toutes les règles doivent être configurables ».
- **Décision** : `fee_rules` (`supabase/migrations/0006_fee_rules.sql`) porte toutes les règles ; chaque colonne optionnelle (`country`, `currency`, `min_amount`/`max_amount`, `source_type`, `destination_type`, `provider`, `transaction_type`, `user_tier`) vaut NULL comme joker. La règle active la plus spécifique (le plus de dimensions contraintes) qui correspond à la requête est retenue (`src/domains/payments/fee-engine/match-rule.ts`, pur et testé unitairement). Une seule règle est semée à ce stade — le taux 3,5 % XOF déjà documenté (ADR précédent) — devenue une donnée configurable au lieu d'une constante TypeScript.
- **`feePayer`** : la règle porte un `fee_payer` par défaut, mais l'appelant peut le surcharger explicitement (`feePayerOverride`) — reflète le choix utilisateur documenté (architecture générale, section 28 : « Qui paie les frais ? »), pas encore exposé dans une UI (Send Money, Prompt 13).
- **Statut** : Adopté.
- **Accès** : aucune policy RLS cliente sur `fee_rules` — lecture/écriture réservées au `service_role`, faute d'UI Back Office de tarification (Prompt 22).
- **Vérifié empiriquement** (tests d'intégration) : le taux de repli à plusieurs montants (1000/5000/10000/250000 FCFA), `feePayerOverride`, une règle fournisseur plus spécifique l'emportant sur le taux générique, le respect d'une plage de montant, et l'absence de règle correspondante (devise inconnue) levant `NoMatchingFeeRuleError`.

## ADR-035 — Utilitaire `pickMostSpecific` partagé entre Fee Engine et Limit Engine

- **Date** : 2026-08-25
- **Contexte** : Fee Engine (Prompt 10) et Limit Engine (Prompt 11) partagent exactement le même principe de résolution — parmi les règles configurables qui correspondent à une requête, retenir la plus spécifique.
- **Décision** : extraction dans `src/domains/payments/shared/pick-most-specific.ts` (générique, `<T>`), réutilisé par `fee-engine/match-rule.ts` (refactorisé sans changement de comportement, 18 tests existants toujours au vert) et `limit-engine/match-rule.ts`. La logique de correspondance elle-même (`ruleMatches`, dimensions différentes par moteur) reste propre à chaque domaine — seul le « retenir le plus spécifique » est partagé.
- **Statut** : Adopté.

## ADR-036 — Limit Engine : table vide au départ, absence de règle ≠ refus

- **Date** : 2026-08-25
- **Contexte** : contrairement au Fee Engine (taux 3,5 % documenté), aucune valeur de limite n'est définie dans les documents source du projet.
- **Décision** : `limit_rules` (`supabase/migrations/0007_limit_rules.sql`) n'est semée d'aucune règle. Le Limit Engine traite l'absence de règle pour un type donné comme « aucune contrainte », jamais comme un refus — conforme à la règle du Master Prompt « si une information n'est pas définie, ne l'invente pas ». Quatre types de limite supportés : `per_transaction_amount`, `daily_amount`, `monthly_amount` (cumul sur la période calendaire courante, UTC), `frequency_count` (nombre d'opérations sur une fenêtre glissante configurable en heures). Chaque règle peut se spécialiser par pays/devise/statut KYC/fournisseur/type de transaction/palier utilisateur (mêmes jokers NULL que `fee_rules`).
- **Décision explicable** : `checkLimits` retourne `{ allowed, violations[] }` où chaque violation détaille la règle, le plafond et l'usage projeté — jamais un simple booléen opaque, conformément à l'exigence explicite du Prompt 11.
- **Calcul de l'usage** : lit directement `transactions` (statuts exclus : `failed`/`rejected`/`cancelled`/`expired` — tout le reste, y compris les statuts en cours, compte, de façon volontairement conservatrice).
- **Jamais de blocage frontend uniquement** : le moteur s'exécute exclusivement côté serveur (Payment Orchestrator, `service_role`) — aucune policy RLS cliente sur `limit_rules`.
- **Statut** : Adopté.
- **Vérifié empiriquement** : absence de règle toujours autorisée ; refus par `per_transaction_amount` avec décision explicable ; `daily_amount` tenant compte de l'usage réellement réglé ; `frequency_count` refusant après N opérations réelles sur la fenêtre ; une règle KYC-spécifique l'emportant sur la règle générique ; bout-en-bout dans l'orchestrateur (`LIMIT_ERROR` → transaction `failed`).

## ADR-037 — Tests d'intégration en série (`fileParallelism: false`)

- **Date** : 2026-08-25
- **Contexte** : `fee_rules` et `limit_rules` sont des configurations **globales**, non scopées par utilisateur. Vitest exécute les fichiers de test en parallèle par défaut ; une règle temporaire posée par un fichier peut donc fausser un test d'un autre fichier tournant au même instant contre le même projet Supabase partagé.
- **Décision** : `fileParallelism: false` dans `vitest.config.mts`. Les fichiers de test s'exécutent en série (plus lent, ~80 s pour 64 tests contre ~40 s en parallèle) mais sans interférence.
- **Statut** : Adopté.
- **Trouvé et corrigé pendant la vérification** : le test « portefeuille → portefeuille » de `orchestrator.test.ts` échouait par intermittence avec `LIMIT_ERROR`, provoqué par la règle `frequency_count` temporaire du test `check-limits.test.ts` exécuté en parallèle sur un utilisateur totalement différent — pas un bug du Limit Engine lui-même, mais une leçon sur l'isolation des tests d'intégration face à une configuration globale partagée.

## ADR-038 — Ledger : immuabilité réelle de `ledger_entries`, y compris pour `service_role`

- **Date** : 2026-08-25
- **Contexte** : le Prompt 12 exige un Ledger « append-only » comme source comptable de vérité (`docs/ARCHITECTURE.md`, section 11). En production, l'application n'accède jamais à la base autrement que via `service_role` — si ce rôle pouvait modifier ou supprimer une écriture, la garantie d'immuabilité serait purement conventionnelle, pas réelle.
- **Décision** : `supabase/migrations/0008_ledger.sql` ajoute un trigger (`forbid_ledger_entries_mutation`) qui bloque UPDATE et DELETE sur `ledger_entries` pour **tout rôle**, y compris `service_role`. Toute correction (erreur, litige) doit être une nouvelle écriture de type `reversal`/`refund`, jamais une réécriture.
- **Statut** : Adopté.
- **Conséquence assumée sur les tests d'intégration** : une transaction créée pendant un test qui atteint réellement le règlement (`recordSettlement`) garde définitivement ses écritures et son propre enregistrement `transactions` en base (la contrainte de clé étrangère empêche de supprimer une transaction référencée par des écritures qu'on ne peut plus supprimer) — exactement comme en production. `orchestrator.test.ts` et `ledger/record-entries.test.ts` ne suppriment donc en fin de test que les transactions qui n'ont **jamais** été réglées ; les autres restent en base, au même titre que le compte de démonstration `demo.naminto.ex@example.test` laissé volontairement en place.

## ADR-039 — Ledger : mapping compte source/destination dérivé de `source_type`/`destination_type`

- **Date** : 2026-08-25
- **Contexte** : aucun document source ne précise explicitement quel compte interne du grand livre représente chaque combinaison possible de `source_type`/`destination_type`/`provider` d'une transaction — seul le principe général (comptabilité en partie double) est documenté.
- **Décision** (`src/domains/payments/ledger/record-entries.ts`) : `naminto_wallet` → compte `user_wallet` (scopé par `sender_user_id`/`recipient_user_id`) ; `linked_account` → compte `provider_suspense` (scopé par `provider`, représente l'argent en transit sur le rail du fournisseur) ; `destination_type = external` (hors Naminto.Ex et hors fournisseur modélisé) → compte générique `external_suspense`. Les frais, quand ils existent, créditent toujours un compte `fee_revenue` global par devise.
- **Statut** : Adopté à titre de choix d'implémentation raisonnable — **TODO_DECISION** si un plan comptable plus précis (un compte de transit par fournisseur *et* par pays, par exemple) est requis plus tard.

## ADR-040 — Ledger : garde-fous applicatifs avant les contraintes base (montant, devise, équilibre)

- **Date** : 2026-08-25
- **Contexte** : `ledger_entries` porte déjà une contrainte `amount > 0` côté base, mais rien ne garantissait qu'un lot d'écritures reste équilibré (Σdébits = Σcrédits) ni homogène en devise avant l'insertion.
- **Décision** : `writeBalancedEntries` (fonction interne unique par laquelle transitent `recordSettlement`/`recordReversal`/`recordRefund`) valide le lot avant tout appel réseau — montant strictement positif (`LedgerInvalidAmountError`), devise unique par lot (`LedgerCurrencyMismatchError`), équilibre débit/crédit (`LedgerImbalanceError`) — même logique de défense en profondeur que la State Machine de transaction (ADR-026).
- **Statut** : Adopté.
- **Vérifié empiriquement** (tests d'intégration `ledger/record-entries.test.ts`) : frais payés par l'expéditeur/le destinataire (écritures équilibrées dans les deux cas) ; double appel à `recordSettlement`/`recordRefund` sans deuxième lot (idempotence) ; montant ≤ 0, devises mêlées et lot déséquilibré tous rejetés avant écriture ; transaction inexistante rejetée pour les trois fonctions ; `recordReversal` sans règlement préalable rejeté (`LedgerMissingSettlementError`) ; écritures miroir d'un reversal strictement inverses de celles du règlement, compte par compte.

## ADR-041 — Send Money : deux combinaisons couvertes, `feePayerOverride` et référence externe enfin branchés

- **Date** : 2026-08-25
- **Contexte** : construire l'UI Send Money (Prompt 13) a mis en évidence que le Payment Orchestrator (Prompts 09-11), tel que testé jusqu'ici, ne couvrait réellement que deux combinaisons source/destination — `naminto_wallet → naminto_wallet` (portefeuille à portefeuille) et `linked_account → external` (débit d'un compte lié du sender, vérifié par `orchestrator.test.ts`). Une troisième combinaison, `naminto_wallet → external` (envoyer depuis son solde Naminto.Ex directement vers un numéro externe quelconque), donnerait `route.provider = null` dans `routing.ts` : la transaction se réglerait sans jamais appeler un fournisseur, ce qui ne correspond à aucune règle documentée sur la façon dont Naminto.Ex disperse réellement des fonds vers un rail mobile money externe.
- **Décision** : l'UI Send Money n'expose que les deux combinaisons réellement couvertes. Envoyer vers un bénéficiaire externe implique de choisir l'un de ses **propres** comptes liés comme source (mêmes contraintes que le Prompt 06 : Naminto.Ex ne connaît que les comptes de l'utilisateur connecté, jamais ceux d'un tiers). `naminto_wallet → external` et `linked_account → naminto_wallet` restent **TODO_DECISION** — non exposés tant que le modèle de disbursement externe (comment Naminto.Ex crédite un rail mobile money quelconque sans compte lié intermédiaire) n'est pas défini par une spécification.
- **`feePayerOverride`** : `PaymentRequest` (orchestrator-steps/types.ts) n'avait jamais ce champ, et `orchestrator-steps/fee.ts` ne le passait donc jamais à `calculateFee` — le choix « qui paie les frais ? », déjà prévu côté Fee Engine depuis l'ADR-034, n'avait tout simplement aucun appelant. Ajouté au type et propagé ; testé par un nouveau cas dans `orchestrator.test.ts` (transaction wallet-à-wallet avec `feePayerOverride: "recipient"`, `fee_payer` persisté vérifié).
- **`destination_external_reference`** (`supabase/migrations/0009_send_money.sql`) : `transactions.destination_reference` est un `uuid` référençant `linked_accounts` (0005_transactions.sql) — incompatible avec un numéro de téléphone en texte libre pour un bénéficiaire externe (qui n'a par construction aucune ligne `linked_accounts`, Naminto.Ex ne le connaît pas). Nouvelle colonne texte dédiée plutôt qu'un réemploi incompatible du type existant.
- **Bug trouvé et corrigé en marge** : `orchestrator.ts` assignait toujours `destinationReference: request.destinationLinkedAccountId` à la création de la transaction, y compris pour `destinationType = 'external'` — la référence du bénéficiaire externe n'était donc jamais persistée nulle part avant ce prompt (aucun appelant réel n'avait encore exercé ce chemin). Corrigé : `destinationReference` reste réservé au cas `linked_account`, `destinationExternalReference` est désormais dérivé et persisté séparément pour le cas `external`. Repéré en écrivant `sendMoneyAction`, pas par relecture de code — `orchestrator.test.ts` échouait avec `invalid input syntax for type uuid` en repassant un numéro de téléphone dans `destinationLinkedAccountId`.
- **QR** : la spécification mentionne un scan QR pour identifier le bénéficiaire, mais aucune capture caméra n'est implémentée (non testable dans cet environnement de développement) — l'identifiant Naminto.Ex se saisit en texte, avec une mention « bientôt disponible ». N'affaiblit pas l'exigence « un scan QR ne doit jamais exécuter automatiquement » : aucune UI de scan n'existe pour la violer, et le Récapitulatif reste de toute façon un point de confirmation explicite obligatoire.
- **Statut** : Adopté. La migration 0009 a été appliquée au projet Supabase réel (Session Pooler, identifiants redonnés par l'utilisateur pour cette seule opération).

## ADR-042 — Request Money : jeton de capacité plutôt que RLS publique, échéance 7 jours, origine absolue fiable

- **Date** : 2026-08-26
- **Contexte** : le Prompt 14 exige un identifiant « sécurisé et expirant » pour Request Money, partageable par lien et QR, sans jamais placer d'information secrète dans l'URL ou le QR.
- **Décision** : `money_requests.token` (`crypto.randomUUID()`, même générateur que `idempotencyKey` ailleurs dans ce domaine) sert de **jeton de capacité** — c'est lui, pas une policy RLS, qui contrôle l'accès en lecture depuis un lien partagé. Aucune policy RLS `select` publique n'existe sur `money_requests` : un visiteur qui ne connaît pas le jeton ne peut pas énumérer les demandes d'un tiers, y compris authentifié. La résolution par jeton passe par `service_role` (`getMoneyRequestByToken`), qui ne renvoie jamais que les champs nécessaires à l'affichage.
- **Échéance par défaut** : 7 jours (`MONEY_REQUEST_TTL_MS`), valeur raisonnable non documentée dans les sources — **TODO_DECISION** si une autre durée est requise (voir tableau ci-dessous).
- **Statut effectif calculé, jamais écrit physiquement** : une ligne `pending` dont `expires_at` est dépassé est traitée comme `expired` à la lecture (`effectiveStatus`), sans tâche planifiée pour réécrire les lignes en base — cohérent avec l'absence de dépendance à un job hors périmètre de ce prompt.
- **Idempotence du règlement** : `fulfillMoneyRequest` utilise un `idempotencyKey` déterministe (`money-request-${token}`) pour l'appel au Payment Orchestrator, puis marque la demande `fulfilled` par une mise à jour **conditionnelle** (`.eq('status', 'pending')`) pour ne jamais écraser un règlement déjà enregistré par une tentative concurrente. Un rejeu par le **même** payeur (double clic, coupure réseau) renvoie la transaction déjà créée sans rappeler l'orchestrateur ; un rejeu par un payeur **différent** sur une demande déjà réglée est rejeté.
- **Origine absolue du lien de partage** : la première version utilisait `headers().get("origin")` (repris de `registerAction`, où c'est fiable car déclenché par une soumission de formulaire). Sur une navigation GET classique vers `/request/[id]`, le navigateur n'envoie pas d'en-tête `Origin` — le lien affiché était donc relatif (`/pay/<token>`) et donnait un QR invalide. Corrigé via `src/lib/request-origin.ts`, qui reconstruit l'origine à partir de `Host`/`x-forwarded-host` (toujours présents), avec `x-forwarded-proto` pour le protocole. Repéré en ouvrant réellement le lien affiché, pas par relecture de code.
- **Statut** : Adopté. Migration `0010_money_requests.sql` appliquée au projet Supabase réel (Session Pooler, identifiants redonnés par l'utilisateur pour cette seule opération, comme pour la migration 0009).
- **Vérifié empiriquement** : cycle de vie complet (création, listing scopé RLS, annulation avec garde de statut, auto-paiement bloqué, règlement réel via un second compte de test, idempotence du rejeu, refus sur demande annulée/expirée/inexistante) — 12 tests d'intégration. `listOwnMoneyRequests` (client RLS, `cookies()`) n'est testable que dans un vrai contexte de requête Next.js, comme `getLinkedAccounts`/`getIdentityProfile` ailleurs dans ce dépôt — vérifié manuellement dans le navigateur plutôt que par Vitest.

## ADR-043 — QR Engine : interprétation des 4 types, signature HMAC, pas de scanner caméra

- **Date** : 2026-08-26
- **Contexte** : le Prompt 15 nomme quatre types de QR (`BENEFICIARY`, `REQUEST`, `PAYMENT_REQUEST`, `PREFILLED_PAYMENT`) et exige un cycle `decode → validate → resolve → display → confirm → authenticate → execute`, jamais `scan → execute`, sans élaborer davantage sur la sémantique de chaque type — vérifié en relisant `PAYMENTS — Spécification Markdown.docx` (reconstitution du 2026-08-25, elle-même dérivée du même prompt) : aucun détail supplémentaire n'existe nulle part dans les documents source.
- **Décision — sémantique retenue** :
  - `BENEFICIARY` : identifie un utilisateur Naminto.Ex (`naminto_id`), aucun montant — remplace le QR en clair de `/receive` (Prompt 14).
  - `PAYMENT_REQUEST` : instantané enrichi (montant, devise, demandeur) d'une demande d'argent existante (`money_requests`, Prompt 14) — utilisé par `/request/[id]`.
  - `REQUEST` : référence minimale (jeton seul) du même objet `money_requests` — traité comme une variante plus légère de `PAYMENT_REQUEST`, pleinement prise en charge par le moteur (decode/validate/resolve, redirection vers `/pay/[token]`) mais sans point de génération dans l'UI de ce dépôt, faute de second cas d'usage produit clairement distinct à ce stade.
  - `PREFILLED_PAYMENT` : paiement à montant fixe vers un bénéficiaire précis, **sans** ligne `money_requests` — seul type réellement nouveau par rapport à ce qui existait déjà (Prompts 13-14), ajouté sur `/receive` (« Demander un montant précis »).
  - Ce choix privilégie la réutilisation maximale de l'existant (Send Money, `/pay/[token]`) plutôt que de dupliquer une UI de paiement pour chaque type — cohérent avec la règle du Master Prompt « ne duplique aucun composant/système ». Si une distinction produit plus précise entre `REQUEST` et `PAYMENT_REQUEST` est requise, c'est un TODO_DECISION (voir tableau ci-dessous).
- **Signature** : HMAC-SHA256 avec un secret généré localement (`crypto.randomBytes(32)`, `QR_SIGNING_SECRET` dans `.env.local`, jamais commité — voir ADR sur les secrets, section Sécurité générale). Format compact `<payload base64url>.<signature base64url>`, vérifié en temps constant (`timingSafeEqual`) pour ne pas exposer de canal auxiliaire de comparaison. Un payload correctement signé mais expiré, ou dont la forme ne correspond pas au type déclaré, est rejeté avant toute résolution en base — la signature prouve l'origine (« émis par Naminto.Ex »), jamais que le contenu est encore d'actualité (rôle de `resolve.ts`, toujours exécuté séparément).
- **Aucun scanner caméra** : comme au Prompt 13 (ADR-041), la capture caméra n'est pas implémentée — non testable dans cet environnement de développement. Contrairement au Prompt 13 cependant, ce n'est pas vraiment une limitation produit : chaque QR généré par ce moteur encode une URL ordinaire (`/qr/<encoded>` ou `/pay/<token>`), donc lisible par n'importe quelle application d'appareil photo standard d'un téléphone — un scanner intégré à l'application serait un raccourci UX, pas une fonctionnalité bloquante pour que le QR Engine soit réellement utilisable.
- **`PREFILLED_PAYMENT` et idempotence sans ligne persistée** : n'ayant pas de `money_requests.id`, `idempotencyKey` est dérivée d'un hash du QR brut et de l'id du payeur (`qr-prefilled-<hash>`) plutôt qu'un identifiant de ligne — un même payeur qui rejoue la confirmation obtient toujours la même transaction ; deux payeurs différents scannant le même QR statique (réutilisable jusqu'à expiration) obtiennent chacun la leur, comme attendu d'un QR de paiement réutilisable.
- **Statut** : Adopté.
- **Vérifié empiriquement** : 13 tests (signature/altération/expiration/payload implausible pour `encodeQr`/`verifyQr`, decode→validate→resolve réel contre le vrai projet Supabase pour `BENEFICIARY` et `PREFILLED_PAYMENT`) ; bout-en-bout manuel contre le vrai projet — `PREFILLED_PAYMENT` payé par un second compte réel (transaction settled, montant et frais corrects), `BENEFICIARY` redirigeant vers Send Money avec bénéficiaire pré-vérifié, `PAYMENT_REQUEST` redirigeant vers `/pay/[token]`, QR expiré et QR à signature falsifiée tous deux rejetés avec un message distinct.

## ADR-044 — History : le reçu recalcule débit/crédit comme le Ledger, ne lit pas `transactions.total`

- **Date** : 2026-08-26
- **Contexte** : le Prompt 16 exige que « le reçu soit cohérent avec le Ledger ». En construisant la page détail (`/history/[reference]`), relire `transactions.total` pour afficher le montant réellement débité de l'expéditeur aurait été trompeur : `createTransaction` (`src/domains/payments/transactions.ts`, Prompt 08) fixe `total = amount + fee` **inconditionnellement**, une formule antérieure au `fee_payer` introduit au Prompt 10 et réellement branché au Prompt 13. Quand `fee_payer = 'recipient'`, le montant réellement débité de l'expéditeur est `amount` (pas `amount + fee`) et le montant réellement crédité au destinataire est `amount - fee` — exactement ce que `recordSettlement` (`src/domains/payments/ledger/record-entries.ts`, Prompt 12) calcule pour les écritures Ledger, mais pas ce que `total` reflète.
- **Décision** : la page détail et le reçu recalculent `senderDebit`/`recipientCredit` avec la même formule exacte que `recordSettlement`, jamais depuis `total`. `total` reste inchangé en base (aucune migration nécessaire, aucun appelant existant n'en dépendait pour un calcul fee_payer-sensible) — seul son usage pour l'affichage du reçu était concerné.
- **Trouvé en construisant ce prompt**, pas par relecture de code isolée : en vérifiant manuellement un envoi réel avec `fee_payer = sender` (750 XOF + 26,25 XOF de frais), afficher `total` aurait donné le même résultat par coïncidence (`total = senderDebit` dans ce cas précis) — c'est en écrivant délibérément le test symétrique pour `fee_payer = recipient` que la divergence entre `total` et `senderDebit` réel est devenue visible.
- **Statut** : Corrigé (dans l'affichage seulement — `transactions.total` n'est pas modifié).
- **Conséquence** : si un futur écran a besoin d'afficher un montant réellement débité/crédité (au-delà du reçu), il doit utiliser la même formule dérivée de `fee_payer`, pas `total`, sous peine de répéter cette même divergence.

## ADR-045 — Risk Engine : seuils en constantes, agrégation sans composition, calibrage contre la suite de tests réelle

- **Date** : 2026-08-26
- **Contexte** : le Prompt 17 exige d'analyser sept dimensions (amount, frequency, history, device, beneficiary, behavior, context) et de renvoyer LOW/MEDIUM/HIGH avec des raisons structurées, sans jamais écrire dans le Ledger. Aucune valeur de seuil n'est documentée nulle part dans les sources du projet — comme pour le Limit Engine (ADR-036) et le Fee Engine (ADR-034), des valeurs raisonnables ont dû être choisies par implémentation.
- **Configurabilité** : contrairement au Fee Engine (Prompt 10) et au Limit Engine (Prompt 11), dont l'énoncé exige explicitement « toutes les règles doivent être configurables », le Prompt 17 ne le demande pas. Décision : les seuils du Risk Engine restent des constantes de code (`assess-risk.ts`), pas une table `risk_rules` — cohérent avec la lettre du prompt, pas une omission. **TODO_DECISION** si une configurabilité en base est requise plus tard (voir tableau ci-dessous).
- **Agrégation sans règle de composition** : la première version escaladait automatiquement la décision à HIGH dès que 3 signaux ou plus étaient MEDIUM simultanément. Trouvé problématique en vérifiant contre la vraie suite de tests d'intégration existante (Prompts 08-16) : un compte de test fraîchement créé (par construction, sans historique) effectuant un envoi externe de 250 000 XOF — exactement le scénario du test `COMPLIANCE_REJECTION` déjà en place — cumule à lui seul les signaux `history` (compte sans historique), `beneficiary` (nouveau bénéficiaire) et `context` (sortie externe pour un montant élevé), tous MEDIUM : la composition aurait fait passer la décision à HIGH et bloqué la transaction **avant même** que Compliance n'ait l'occasion de la rejeter pour la raison que ce test vérifie précisément — cassant un test déjà correct pour une raison sans rapport. Décision : la décision globale retient uniquement le signal le plus sévère, sans composition — un seul signal HIGH suffit à bloquer, mais aucun nombre de signaux MEDIUM ne s'y substitue. La combinaison de plusieurs signaux modérés en une action (bloquer, step-up, revue manuelle) est explicitement le rôle du Fraud Engine (Prompt 18 : « implémente une architecture de règles », actions `ALLOW`/`STEP_UP`/`BLOCK`/`MANUAL_REVIEW`) — pas une responsabilité à anticiper dans le Risk Engine lui-même.
- **Seuil de fréquence recalibré pour la même raison** : `frequency` compte les opérations de l'expéditeur sur la dernière heure (réutilise `getFrequencyUsage`, Limit Engine). Plusieurs fichiers de test créent 8 à 10 transactions pour le même utilisateur de test en quelques dizaines de secondes (usage légitime de test, pas un usage frauduleux) — un seuil HIGH à 5 aurait déclenché `RISK_REJECTION` au milieu de suites de tests sans rapport avec le Risk Engine. Seuils retenus : MEDIUM à partir de 5, HIGH à partir de 15 — cohérents avec un usage réel plausible (quelqu'un qui paie plusieurs factures à la suite) tout en couvrant la suite de tests existante avec une marge confortable.
- **`device` jamais pénalisé par absence d'information** : `deviceFingerprint` est optionnel sur `PaymentRequest` ; quand il est absent, le signal `device` reste `LOW` (« non transmis à l'évaluation du risque »), jamais `MEDIUM` par défaut — ne pas confondre « nous n'avons pas vérifié » avec « nous avons vérifié et trouvé un problème ».
- **`deviceFingerprint` threadé jusqu'à l'orchestrateur** : `PaymentRequest` n'avait jamais ce champ avant ce prompt. Ajouté et renseigné par les trois appelants réels (`sendMoneyAction`, `fulfillMoneyRequest`/`payMoneyRequestAction`, `payPrefilledQrAction`) via `getOrCreateDeviceCookie()` (Identity, Prompt 04) — le même cookie httpOnly déjà utilisé à la connexion pour distinguer les appareils, aucun nouveau mécanisme de suivi introduit.
- **Trouvé et corrigé en marge** : `payMoneyRequestAction` (Prompt 14) et `payPrefilledQrAction` (Prompt 15) ne classifiaient que `AUTH_ERROR` correctement — tout autre code d'erreur de l'orchestrateur (`RISK_REJECTION`, `COMPLIANCE_REJECTION`, `LIMIT_ERROR`, `PROVIDER_ERROR`, `TIMEOUT`) retombait sur le message générique « erreur technique ». Resté invisible jusqu'ici car aucun de ces codes n'était auparavant atteignable pour ces deux parcours (Risk ne rejetait jamais, et les autres codes sont rares dans leurs scénarios). Corrigé en réutilisant la même table de correspondance que Send Money.
- **Statut** : Adopté.
- **Vérifié empiriquement** : 22 tests (7 signaux purs + agrégation, sans composition, en unitaire ; `fetchRiskContext`/`assessRisk` contre le vrai Supabase — historique, bénéficiaire, appareil réels) ; suite complète (133 tests) toujours au vert après branchement réel dans l'orchestrateur ; bout-en-bout manuel — un envoi de 600 000 XOF réellement bloqué avec le message attendu, transaction persistée `failed`, avant que Compliance n'intervienne.

## ADR-046 — Fraud Engine : deux nouveaux codes d'erreur, pipeline réordonné (Fraud après Compliance/Limits), règles en code

- **Date** : 2026-08-26
- **Contexte** : le Prompt 18 exige une architecture de règles (ID, description, severity, condition, action) produisant `ALLOW`/`STEP_UP`/`BLOCK`/`MANUAL_REVIEW`, avec des décisions explicables. Il complète directement le TODO explicitement laissé ouvert au Prompt 17 (ADR-045) : combiner plusieurs signaux Risk modérés en une action revient au Fraud Engine, pas au Risk Engine.
- **Deux codes d'erreur ajoutés** : les 8 codes originaux (Prompt 09) n'incluent aucun code fraude dédié. `FRAUD_BLOCKED` (action BLOCK) et `MANUAL_REVIEW_REQUIRED` (action MANUAL_REVIEW) ajoutés à `OrchestratorErrorCode` — `MANUAL_REVIEW_REQUIRED` conçu pour être réutilisé tel quel par la future revue manuelle de Compliance (Prompt 19 : « aucune vérification ; KYC standard ; KYC renforcée ; revue manuelle »), plutôt que de dupliquer un code équivalent à ce moment-là.
- **`STEP_UP` ne bloque jamais** : aucune authentification supplémentaire (biométrie/WebAuthn) n'existe dans ce dépôt — TODO_DECISION Identity déjà documenté. Une règle qui matche avec l'action `STEP_UP` produit une trace d'audit (`security_events`, type `fraud_rule_matched`) mais laisse la transaction continuer, exactement le même traitement déjà appliqué au step-up documenté pour un nouvel appareil à la connexion (Prompt 04) — pas une nouvelle exception inventée, la même décision assumée deux fois pour la même raison.
- **Pipeline réordonné : Risk → Compliance → Limits → Fraud**, et non Risk → Fraud → Compliance → Limits comme placé initialement. Trouvé en vérifiant réellement contre la suite de tests existante : la règle `FRAUD-002` (3 signaux MEDIUM simultanés ⇒ MANUAL_REVIEW) se déclenchait pour le test `COMPLIANCE_REJECTION` existant (250 000 XOF, compte de test) — un montant élevé sur un compte neuf combine mécaniquement `amount`, `history` et `beneficiary` (ou `behavior`, selon l'historique déjà accumulé par le compte de test partagé) en trois signaux MEDIUM, faisant basculer la décision en MANUAL_REVIEW **avant** que Compliance n'ait eu l'occasion de rejeter pour son propre motif (seuil KYC) — cassant un test déjà correct pour une raison sans rapport avec ce qu'il vérifie. Décision : Compliance et Limits sont des portes déterministes (seuil fixe, plafond configuré) et passent en premier ; Fraud, analyse combinatoire de signaux heuristiques, est la dernière porte avant l'exécution — cohérent avec le principe que les contrôles réglementaires/produit absolus priment sur une détection de motif.
- **`FRAUD-001` volontairement basé sur `frequency = MEDIUM`, jamais `HIGH`** : une fréquence `HIGH` fait déjà basculer `riskDecision.level` à `HIGH`, auquel cas l'orchestrateur rejette directement en `RISK_REJECTION` avant même d'appeler le Fraud Engine — une règle dupliquant cette condition ne serait jamais atteignable. Repéré en écrivant le premier jet de `FRAUD-001` (condition initiale : `riskDecision.level === 'HIGH'`), avant de réaliser qu'elle était strictement inatteignable en pratique (shadowée par le contrôle Risk direct) — corrigé en composant `frequency = MEDIUM` avec `amount ≠ LOW`, un motif que ni Risk ni Compliance ne couvrent seuls.
- **Règles en code, pas en table configurable** — même choix et même justification que le Risk Engine (ADR-045) : le Prompt 18 n'exige pas explicitement une configuration en base, contrairement au Fee Engine et au Limit Engine. **TODO_DECISION** si une configurabilité est requise plus tard.
- **Statut** : Adopté.
- **Vérifié empiriquement** : 24 tests (règles pures, agrégation par action la plus restrictive, bout-en-bout orchestrateur avec deux comptes de test dédiés — un scénario `MANUAL_REVIEW_REQUIRED` et un scénario `FRAUD_BLOCKED` construit via 5 règlements réels suivis d'une sixième tentative) ; suite complète (144 tests) toujours au vert après réordonnancement du pipeline ; envoi normal vérifié dans le navigateur sans interférence, aucune régression sur le chemin `ALLOW`.

## ADR-047 — Compliance Engine : moteur configurable en base, statut KYC binaire assumé pour les deux paliers

- **Date** : 2026-08-26
- **Contexte** : le Prompt 19 remplace le seuil codé en dur `ENHANCED_KYC_THRESHOLD_XOF = 200_000` (posé au Prompt 09 comme STUB) par un vrai moteur, avec `NONE`/`KYC_STANDARD`/`KYC_ENHANCED`/`MANUAL_REVIEW` et une distinction explicite `PRODUCT_RULE`/`REGULATORY_RULE`/`CONFIGURATION`. Contrairement au Risk Engine (ADR-045) et au Fraud Engine (ADR-046), l'énoncé exige ici une configuration en base — même famille que le Fee Engine (ADR-034) et le Limit Engine (ADR-036).
- **`compliance_rules` configurable, pattern déjà éprouvé réutilisé tel quel** : mêmes dimensions à `NULL`-comme-joker, même utilitaire partagé `pickMostSpecific` (ADR-035) que Fee/Limit — aucune nouvelle abstraction inventée pour ce moteur.
- **Règle seedée reproduisant exactement l'ancien comportement** : une unique `REGULATORY_RULE` XOF, `min_amount = 200 000,01`, `requirement = KYC_ENHANCED` — le `.01` réplique la sémantique stricte de l'ancien `amount > 200_000` (et non `>=`), pour qu'aucune transaction déjà couverte par les tests existants ne change de verdict lors de la bascule. Confirmé par les 13 tests `orchestrator.test.ts` existants, inchangés, toujours au vert.
- **`rule_type` documentaire, aucune branche logique dessus à ce stade** : `PRODUCT_RULE`/`REGULATORY_RULE`/`CONFIGURATION` sont stockés et retournés (utile pour l'audit et une future UI Back Office), mais `determineRequirement` ne traite pas un type différemment d'un autre — seule la spécificité des dimensions arbitre entre règles concurrentes. Aucune exigence dans le prompt ne demande un traitement différencié par type.
- **`KYC_STANDARD` et `KYC_ENHANCED` appliqués de façon strictement identique** : `identity_profiles.kyc_status` est un champ binaire (`verified`/`unverified`/...) posé au Prompt 05, aucun palier de vérification n'existe. Les deux niveaux exigent donc aujourd'hui `kyc_status === 'verified'`, sans distinction de pièces ou de contrôles supplémentaires. **TODO_DECISION** si une distinction réelle entre les deux paliers est requise (voir tableau ci-dessous) — introduire un champ de palier KYC dépasse le périmètre du Prompt 19 tel qu'énoncé.
- **`MANUAL_REVIEW` réutilise `MANUAL_REVIEW_REQUIRED`, posé au Prompt 18 par anticipation** (ADR-046 le mentionnait déjà comme réutilisable pour Compliance) — aucun nouveau code d'erreur ajouté.
- **Audit systématique avant arbitrage** : toute exigence non `NONE` produit un `security_events` (`compliance_requirement_applied`) — y compris si la transaction est ensuite acceptée (KYC déjà vérifié). Même principe que le Fraud Engine (ADR-046) : consigner la règle qui s'est appliquée, pas seulement les rejets.
- **`settings.kyc.threshold` (UI) non reconnecté à la table** : le texte reste « 200 000 FCFA » codé en dur. Risque de dérive documenté et assumé plutôt que corrigé — hors périmètre du prompt (le Prompt 19 porte sur le moteur de décision, pas sur cet écran).
- **Statut** : Adopté.
- **Vérifié empiriquement** : 21 tests (`match-rule.test.ts` — purs ; `determine-requirement.test.ts` — intégration contre le vrai Supabase, avec nettoyage systématique des règles de test) ; suite complète (157 tests) toujours au vert ; `npx tsc --noEmit`, `eslint` et `npm run build` propres.

## ADR-048 — Notification Engine : deux Notification Event câblés, PUSH assumé UNAVAILABLE, correction d'un bug latent post-confirmation

- **Date** : 2026-08-26
- **Contexte** : le Prompt 20 remplace le STUB `notifyTransactionSettled` (posé au Prompt 09) par un vrai Notification Service — architecture Domain Event → Notification Event → Template → Channel Adapter, canaux IN_APP/PUSH/SMS, templates FR/EN, préférences, retries, statut de livraison, historique. L'Event Bus générique (`TransactionCreated`, dead-letter, correlation ID…) est explicitement un prompt séparé (Prompt 26) : ce moteur consomme des appels directs typés, pas un bus.
- **Deux événements câblés, pas davantage** : `transaction_settled` et `transaction_failed`, les deux seuls points déjà nommés dans le pipeline orchestrateur. `money_request_created`, les blocages Fraud/Compliance, le nouvel appareil, etc. restent non câblés — chacun aurait exigé de choisir un wording et un niveau de sévérité propres, hors du périmètre resserré de ce prompt. **TODO_DECISION** (voir tableau) si une couverture plus large est requise.
- **PUSH = UNAVAILABLE, jamais MOCK** : ce dépôt ne modélise aucun jeton d'appareil push ni fournisseur (FCM/APNs). Le simuler en MOCK aurait été une fausse intégration au sens strict du Master Prompt — se présenter comme un canal qui « marche » alors qu'aucune brique (jeton, fournisseur) n'existe pour le faire fonctionner un jour sans travail supplémentaire. UNAVAILABLE échoue honnêtement, une seule fois, sans retry inutile (retenter un canal structurellement absent ne change jamais le résultat).
- **SMS = SANDBOX, jamais REAL** : simulé et journalisé (`console.info`), même principe que les adapters `providers/sandbox/*.ts` — aucune clé de fournisseur SMS en environnement. Déterministe (pas d'échec aléatoire simulé), même choix que le Fee/Limit/Compliance Engine : un test ne doit jamais dépendre du hasard.
- **Bug latent corrigé en écrivant ce prompt** : avant cette étape, `orchestrator.ts` appelait le STUB de notification sans aucune protection ; une future implémentation réelle qui lève une exception à cet endroit (juste après `transitionTransaction(..., "settled")`) remontait jusqu'au bloc `catch` englobant de `runPaymentOrchestrator`, qui tentait alors une transition `settled → failed` (invalide selon la State Machine, donc silencieusement absorbée par `safeTransition`) puis **relançait quand même l'erreur à l'appelant** — une transaction réellement réglée en base pouvait ainsi remonter comme un échec côté Server Action, contredisant directement la contrainte explicite du Prompt 20 (« une panne SMS ne doit jamais annuler une transaction financière déjà confirmée »). Corrigé par un `try/catch` local autour de l'appel, juste après la transition `settled`, qui journalise sans jamais relancer. Défense en profondeur : `sendNotification` elle-même ne lève déjà jamais non plus (tout est capturé en interne), mais le point d'appel dans l'orchestrateur ne doit pas dépendre de cette garantie implicite pour rester correct.
- **Même correctif appliqué symétriquement à `notifyTransactionFailed`** (nouveau, dans le bloc `catch` de l'orchestrateur) : une panne de notification à ce moment ne doit jamais masquer l'erreur d'origine, qui reste seule à remonter à l'appelant.
- **Dispatch parallèle plutôt que séquentiel** : canaux d'une même notification (`Promise.all`) et destinataires d'un même événement (expéditeur/destinataire) — ajouté après qu'un test orchestrateur existant (`FRAUD_BLOCKED`, 5 règlements réels dans un seul test) a dépassé son timeout de 60s une fois le Notification Engine réellement branché (round-trips réseau supplémentaires vers Supabase). Le dispatch parallèle réduit la latence cumulée ; le timeout du test a quand même été relevé à 90s par prudence.
- **Complète un STUB scaffoldé bien avant ce prompt** : `shell/notifications-menu.tsx` (dropdown cloche du header) existait depuis l'Application Shell (Prompt 03), toujours vide — branché ici sur l'historique réel via `(user)/layout.tsx`, sans changer sa structure.
- **Statut** : Adopté.
- **Vérifié empiriquement** : 21 tests (`templates.test.ts` — purs ; `send-notification.test.ts` — intégration contre le vrai Supabase, y compris `retryDelivery`) ; suite complète (167 tests) au vert, y compris tous les tests orchestrateur existants qui déclenchent désormais un vrai passage dans ce moteur ; `tsc`, `eslint` et `npm run build` propres ; vérifié manuellement dans le navigateur (préférences par canal persistées, `/notifications`, dropdown cloche).

## ADR-049 — Naminto Assist : moteur d'intentions déterministe (pas de LLM), réponses en données structurées, tickets via service_role

- **Date** : 2026-08-27
- **Contexte** : le Prompt 21 demande un chatbot intégré capable d'expliquer, guider, rechercher, diagnostiquer, expliquer les frais et les statuts, créer un ticket — avec des interdictions absolues (jamais demander PIN/mot de passe/secret, jamais exécuter seul un transfert, jamais modifier seul une transaction). Le workflow des cas sensibles est explicitement documenté : Naminto Assist → Diagnostic → Dossier support → Agent humain (NAMINTO.EX ARCHITECTURE GENERALE.docx, section 37-38).
- **Aucun fournisseur d'IA connecté ⇒ moteur déterministe, pas une fausse intégration LLM** : ce dépôt ne contient ni clé API, ni SDK, pour aucun fournisseur d'IA (vérifié : aucune variable d'environnement `.env.example`, aucun paquet). Simuler un chatbot « intelligent » sans réel moteur NLU derrière aurait été une fausse intégration au sens strict du Master Prompt (section 3, « zéro fausse intégration ») — exactement le même principe déjà appliqué à PUSH/SMS (Notification Engine, ADR-048). Décision : `detectIntent` est un pattern-matching honnête (mots-clés normalisés, référence `NEX-xxx`, montant détecté), documenté comme tel dans le code, jamais présenté comme une IA. **TODO_DECISION** si une vraie intégration LLM est requise plus tard (voir tableau).
- **Détection de sécurité en priorité absolue, avant toute autre intention** : toute mention de PIN/mot de passe/secret est interceptée en premier, quelle que soit la formulation — y compris une question légitime (« comment changer mon mot de passe ? ») reçoit la même redirection sécurisée plutôt qu'une tentative de réponse conversationnelle, choix délibérément conservateur pour cette catégorie explicitement qualifiée d'« interdiction absolue ». Une tentative d'exécution de transfert (verbe d'ordre + absence de marqueur de question) est bloquée séparément, mais distinguée d'une question « comment » sur le même sujet (qui reste un `guide` légitime) — sinon la question la plus naturelle sur Send Money (« comment envoyer de l'argent ? ») serait elle-même bloquée à tort.
- **Aucune capacité d'écriture financière dans ce domaine** : `src/domains/assist/` n'importe jamais l'orchestrateur de paiement ni aucune fonction de transition de transaction — structurellement incapable d'exécuter ou de modifier une transaction, pas seulement par une règle de prompt. La seule écriture possible dans tout le domaine est `createSupportTicket` (une ligne dans `support_tickets`), jamais appelée automatiquement : toujours un choix explicite du titulaire (bouton « Créer un dossier support »).
- **Réponses structurées, pas de texte pré-rendu côté serveur** : contrairement au Notification Engine (Prompt 20, texte rendu une fois en base dans la langue du profil au moment de l'envoi), Naminto Assist est une conversation synchrone qui doit refléter instantanément le sélecteur FR/EN — un choix purement client (`locale-provider.tsx`, `localStorage`, indépendant de `identity_profiles.preferred_language`). `getAssistResponse` renvoie donc l'intention et les données réelles (montant, statut, référence, code de raison) ; toute la traduction et la composition du message reviennent à `assist-view.tsx`.
- **`support_tickets` en écriture service_role, pas RLS client** : premier jet écrit via le client RLS (`support_tickets_insert_own`), corrigé avant tests pour suivre le même choix que `money_requests` (ADR-042 implicite : les écritures qui pourraient un jour porter une règle métier — ici, un futur routage par catégorie vers une file d'agents — passent par service_role dès le départ). La policy insert cliente créée par erreur a été retirée du fichier de migration et supprimée sur le projet réel avant que tout code ne s'appuie dessus.
- **`diagnoseTransaction`/`listRecentTransactionsForAssist` restent en lecture RLS**, à l'inverse : même raisonnement que `history/queries.ts` (Prompt 16) — la policy `transactions_select_participant` fait déjà exactement le filtrage nécessaire pour une simple lecture par le titulaire, inutile d'élever les privilèges. Conséquence assumée : comme le reste des lectures RLS-scopées de ce dépôt, ces fonctions n'ont pas de test Vitest dédié (`next/headers` ne s'exécute pas hors du runtime Next.js) — vérifiées par la conception des policies et manuellement dans le navigateur, pas par une lacune de rigueur nouvelle.
- **Aucune conversation persistée** : seul le résultat d'une escalade (un ticket) est écrit en base — conforme au principe déjà énoncé dans les sources amont (ne pas stocker inutilement des données personnelles/financières, NAMINTO.EX ARCHITECTURE GENERALE.docx, juste avant la section Naminto Assist).
- **Statut** : Adopté.
- **Vérifié empiriquement** : 24 tests (`intent-detector.test.ts` — purs, avec une suite dédiée aux interdictions absolues ; `respond.test.ts` — intégration contre le vrai Supabase pour les intentions non-RLS, dont `explain_fees` confirmé contre le vrai Fee Engine) ; `tsc`, `eslint` propres.

## ADR-050 — Back Office : mutations séparées de leur wrapper "use server", pagination PostgREST corrigée, KYC sans state machine inventée

- **Date** : 2026-08-27
- **Contexte** : le Prompt 22 construit les 17 routes `/admin/*` déjà scaffoldées (Prompt 03), en réutilisant strictement les domaines métier existants — « ne duplique pas les règles métier dans le Back Office ». RBAC reste explicitement le Prompt 23 (ADR-016) : aucune vérification de rôle n'est ajoutée ici.
- **Toute mutation Back Office séparée de son action `"use server"`** : `revalidatePath` lève `Invariant: static generation store missing` hors du runtime Next.js — en écrivant les tests d'intégration de ce prompt, chaque action qui mélangeait logique métier et `revalidatePath` dans un seul export `"use server"` s'est révélée impossible à tester directement. Corrigé systématiquement : la mutation (validation, écriture, journalisation) vit dans `admin-queries.ts`/`admin-mutations.ts` (sans `"use server"`, testable), l'action ne fait plus qu'appeler cette fonction puis `revalidatePath` si elle a réussi. `fee-engine`/`limit-engine`/`compliance-engine` avaient déjà cette séparation par leur propre convention antérieure (Prompt 10/11/19) ; `identity` (KYC), `assist` (tickets), `countries`, `faq`, `legal` ont été alignés sur ce même modèle.
- **Pagination PostgREST : plafond silencieux à 1000 lignes sans `.range()`** — découvert concrètement en écrivant le test `adminListLedgerAccounts` : un test isolé, avec une devise unique, échouait de façon reproductible (solde attendu 35, obtenu 0). Investigation : `ledger_entries` comptait 1007 lignes réelles au moment du test (accumulées par l'ensemble de la suite d'intégration depuis le Prompt 09), et la lecture `.select("account_id, direction, amount")` sans `.range()` ne renvoyait que les 1000 premières — la ligne du test, insérée en dernier, n'y figurait jamais. Corrigé par une pagination explicite qui lit systématiquement toutes les pages (`adminListLedgerAccounts`, et `adminDeliveryStatusBreakdown` par le même raisonnement préventif, `notification_deliveries` étant elle aussi une table qui croît sans borne). Les autres listes Back Office paginées par filtre/plage (`adminListUsers`, `adminListTransactions`, `adminListTickets`, `adminListSecurityEvents`) n'étaient pas concernées : elles bornent déjà explicitement leur requête.
- **`ledger_entries` reste réellement append-only, y compris pour un test** : première tentative du test `adminListLedgerAccounts` avec un `afterAll` nettoyant ses fixtures — échec systématique (« DELETE interdit sur une écriture existante », contrainte base, ADR-038). Corrigé en suivant exactement le précédent déjà établi par `record-entries.test.ts` (Prompt 12) : aucune tentative de suppression des écritures/comptes/transactions réglées créées en fixture, une devise de test unique par exécution suffit à garantir l'isolation. Des lignes `NEX-VITEST*`/comptes de test créés pendant le développement de ce prompt (avant ce correctif) sont donc restées définitivement en base, comme n'importe quelle transaction réelle.
- **Orphelins de tests cassés avant le refactor "use server"/mutation** : les toutes premières versions des actions Countries/FAQ/Legal/Pricing/KYC (avant la séparation ci-dessus) réussissaient leur écriture puis levaient sur `revalidatePath`, avant que le test ait pu capturer l'id créé pour son propre nettoyage — laissant des lignes orphelines (`limit_rules`/`fee_rules`/`compliance_rules`/`countries`/`faq_entries` de test) dans le projet réel. Repérées indirectement : une régression sur `check-limits.test.ts` (Prompt 11, jamais touché par ce prompt) a révélé qu'une règle `frequency_count` orpheline (max_count 999 999) était retenue à égalité de spécificité avec la règle du test, faussant sa décision. Toutes les lignes orphelines identifiées ont été supprimées une fois la cause comprise.
- **KYC : aucune state machine inventée** — contrairement à `TransactionStatus` (Prompt 08, state machine documentée dans les sources), aucune source ne définit de transitions valides entre les 5 valeurs de `kyc_status`. Plutôt que d'en deviner une, `adminUpdateKycStatus` n'exige qu'un changement réel (`next ≠ actuel`). **TODO_DECISION** si une state machine KYC réelle est requise, probablement au moment de l'intégration d'un fournisseur KYC externe (TODO_DECISION User déjà documenté).
- **Countries/FAQ/Legal : seuls modules sans domaine métier préexistant** — contenu de référence/administratif (pas une règle financière), migration `0014_back_office.sql`, RLS lecture publique restreinte à ce qui est publiable. Aucun des trois n'est encore consommé ailleurs dans le produit (aucun sélecteur pays, Naminto Assist toujours sur ses six thèmes codés en dur, aucune page CGU publique) — **TODO_DECISION** pour chacun.
- **Statut** : Adopté.
- **Vérifié empiriquement** : 16 nouveaux tests (mutations testées directement, hors `"use server"` ; agrégation Ledger et Dashboard ; filtrage Risk/Fraud par préfixe de raison réel, jamais reconstruit) ; suite complète au vert une fois les orphelins nettoyés ; `tsc`, `eslint` et `npm run build` propres ; les 13 modules construits vérifiés manuellement dans le navigateur, dont un aller-retour complet Naminto Assist (Prompt 21) → Back Office Support.

## ADR-051 — RBAC : bootstrap auto-résolu, correspondance rôle→permissions assumée, groupe de routes pour isoler /admin/forbidden

- **Date** : 2026-08-27
- **Contexte** : le Prompt 23 ferme le TODO_DECISION bloquant laissé par ADR-016 — neuf rôles nommés, neuf exemples de permissions explicites (`ressource.action`), contrôle exigé côté serveur.
- **Bootstrap auto-résolu plutôt qu'un compte codé en dur** : aucune source ne désigne quel compte doit devenir le premier administrateur. Décision : tant que `admin_role_assignments` est complètement vide, le tout premier utilisateur authentifié qui accède à `/admin` s'auto-attribue `super_admin` (`getUserRoles`, `src/domains/rbac/queries.ts`). Comportement à usage unique dans la vie du projet — une fois la première ligne posée, plus aucun bootstrap, quel que soit le compte. Vérifié réellement : le compte `demo_naminto` utilisé tout au long de cette session est devenu `super_admin` à son premier accès après la migration.
- **Correspondance rôle → permissions assumée, pas documentée dans les sources** : le prompt nomme les rôles et donne des exemples de permissions, jamais leur association. Choix fait ici (`ROLE_PERMISSIONS`, `src/domains/rbac/types.ts`) : un rôle par domaine du Back Office (ex. `kyc` → `kyc.read`/`kyc.review`/`user.read`, `finance` → `ledger.read`/`pricing.*`), `super_admin` recevant strictement toutes les permissions déclarées, `role.manage` n'étant jamais accordée à un autre rôle. **TODO_DECISION** si le produit exige une correspondance différente.
- **24 permissions, dont 9 reprises telles quelles des exemples du prompt** et 15 supplémentaires étendant le même schéma aux modules du Prompt 22 sans exemple donné (Dashboard, Support, Risk, Fraud, Countries, FAQ, Legal, Notifications). `transaction.review`/`transaction.cancel` sont déclarées mais non câblées à une action réelle : aucun workflow de révision/annulation manuelle de transaction n'existe encore dans le produit (probablement lié à Reconciliation, Prompt 24).
- **Groupe de routes `(protected)`** : toutes les pages gardées ont été déplacées sous `src/app/admin/(protected)/` — sans ce déplacement, `/admin/forbidden` aurait hérité du layout qui redirige justement vers `/admin/forbidden` en l'absence de permission, provoquant une boucle de redirection infinie pour tout compte sans rôle.
- **`user.suspend` câblé à `identity_profiles.status = 'suspended'`**, déjà défini au Prompt 04 mais jusqu'ici jamais atteignable — première action réelle du Back Office à l'utiliser.
- **Statut** : Adopté.
- **Vérifié empiriquement** : 12 tests (taxonomie des permissions ; `getUserRoles`/`getUserPermissions`, notamment la garantie qu'un compte sans rôle ne reçoit jamais de repli implicite une fois la table déjà non vide — le bootstrap lui-même, état global à usage unique, est vérifié manuellement plutôt que dans un test destructif sur le projet partagé ; attribution/retrait de rôle ; suspension/réactivation d'un compte) ; `tsc`, `eslint` et `npm run build` propres ; vérifié de bout en bout dans le navigateur avec un second compte de test limité au rôle `kyc` : navigation filtrée, blocage serveur direct sur une URL non autorisée, action autorisée réellement exécutée.

## ADR-052 — Reconciliation Engine : trois vues comparées par cinq vérifications indépendantes, réconciliation immédiate sans ordonnanceur, jamais une écriture Ledger

- **Date** : 2026-08-27
- **Contexte** : le Prompt 24 ferme le `ComingSoonPage` de Reconciliation laissé au Prompt 22 (ADR-050) — aucun moteur sous-jacent n'existait, RBAC (Prompt 23) ayant déjà déclaré `transaction.review`/`transaction.cancel` sans les câbler, en anticipant ce prompt.
- **Trois vues, pas une comparaison unique** : aucune source ne détaille précisément comment rapprocher un règlement. Décision : LEDGER (écritures réelles), PROVIDER (statut relu via le Provider Gateway existant, Prompt 07) et SETTLEMENT (état de la transaction) sont construites indépendamment (`build*View`, `src/domains/reconciliation/run.ts`), puis comparées deux à deux par cinq fonctions pures (`checks.ts`) — jamais les trois à la fois, chaque type d'anomalie isolant une seule paire pour rester diagnostiquable. `expectedSenderDebit` (SETTLEMENT) est un recalcul en lecture seule à partir de `amount`/`fee`/`fee_payer`, jamais une réécriture de ces colonnes. **TODO_DECISION** si un découpage différent (ex. une vue PROVIDER par étape intermédiaire) est requis.
- **Réconciliation immédiate, pas de lot planifié** : aucun ordonnanceur/cron n'existe dans ce dépôt (même constat que pour l'Availability Engine, Prompt 47, non encore construit). Décision : chaque règlement déclenche `scheduleReconciliation` juste après `settled`, avec le même filet de défense en profondeur que `notifyTransactionSettled` (Prompt 20, ADR-048) — un échec de réconciliation ne doit jamais annuler ni faire échouer une transaction déjà confirmée. Le lot manuel (`runReconciliation`, bouton Back Office) couvre seulement les 200 transactions terminales les plus récentes, pas un balayage complet historique. **TODO_DECISION** si un vrai ordonnanceur est introduit plus tard.
- **Jamais une écriture Ledger** : cohérent avec l'immuabilité déjà actée (ADR-038, DELETE bloqué même pour service_role) — le domaine ne fait qu'insérer des lignes `reconciliation_anomalies` (migration `0016_reconciliation.sql`, sans policy RLS client, service_role uniquement, même schéma que les autres tables de plomberie interne du Prompt 22).
- **Idempotente au niveau anomalie** : une anomalie d'un type donné déjà `open`/`investigating` pour une transaction n'est jamais recréée à un rejeu (vérifié avant insertion) — évite qu'un lot manuel répété ou un règlement rejoué ne produise un doublon.
- **Cycle `open` ↔ `investigating` ↔ `resolved` ↔ `closed`**, avec réouverture possible — reprend tel quel le précédent déjà établi pour les tickets de support (Prompt 21/22), pas un nouveau modèle inventé pour ce prompt.
- **Deux permissions RBAC, assignées à `finance` uniquement** (`reconciliation.read`, `reconciliation.manage`) — cohérent avec la correspondance rôle → domaine déjà assumée pour `finance` (ledger, pricing — ADR-051). **TODO_DECISION** si un autre rôle (ex. `operations`) doit également y avoir accès.
- **Statut** : Adopté.
- **Vérifié empiriquement** : 18 tests (cinq vérifications pures testées sans base de données ; `reconcileTransaction` bout en bout contre le vrai projet Supabase — règlement propre sans anomalie, règlement sans écriture Ledger détecté puis non recréé au rejeu ; transitions de statut d'anomalie, y compris le refus d'une transition hors cycle et d'un identifiant inconnu) ; `tsc`, `eslint` et `npm run build` propres ; vérifié de bout en bout dans le navigateur : lancement manuel depuis `/admin/reconciliation`, transition de statut d'une anomalie, et une nouvelle transaction réglée via Send Money déclenchant automatiquement la réconciliation sans jamais faire échouer l'orchestrateur.

## ADR-053 — Webhooks : signature HMAC SANDBOX partagée, fenêtre anti-rejeu de 5 minutes, aucune transition de transaction déclenchée par un webhook

- **Date** : 2026-08-28
- **Contexte** : le Prompt 25 exige un webhook « authentifié, vérifié, signé si possible, horodaté, idempotent, audité », protégé contre « rejeu, duplication, falsification, payload invalide, webhook hors ordre », avec un mécanisme de rejeu contrôlé — sans détailler l'implémentation. Le point d'entrée `POST /api/webhooks/[provider]` existait depuis le Prompt 07 comme démonstration du contrat `ProviderAdapter`, sans vérification ni persistance réelle.
- **Six propriétés mappées à un mécanisme distinct, pas une seule protection générique** : authentifié/signé/protection falsification → signature HMAC-SHA256 (en-tête `t=<ms>,v1=<hex>`, texte signé = `${t}.${payload}`, même principe que la vérification QR déjà en place — Prompt 15, ADR non renuméroté ici) ; vérifié/protection payload invalide → validation de forme après la signature (`type`/`event_id`/`occurred_at` requis) ; horodaté/protection rejeu → fenêtre anti-rejeu ; idempotent/protection duplication → vérification applicative sur `(provider, event_id)`, pas une contrainte unique en base (pour ne jamais bloquer un rejeu contrôlé légitime portant le même `event_id`) ; audité → une ligne `webhook_events` par requête reçue, acceptée ou non.
- **Un seul secret HMAC partagé par les 5 adapters SANDBOX** (`WEBHOOK_SIGNING_SECRET`) : ce dépôt simule à la fois l'émetteur et le récepteur pour chacun des 5 fournisseurs, donc un schéma de signature uniforme suffit. Un fournisseur REAL aura nécessairement son propre schéma (algorithme, en-tête, format) documenté par lui — **TODO_DECISION** à sa connexion réelle, à implémenter dans son propre adapter sans toucher au pipeline `process-webhook.ts`.
- **Fenêtre anti-rejeu de 5 minutes** (`REPLAY_WINDOW_MS`) — valeur raisonnable non documentée dans les sources, même statut que `MONEY_REQUEST_TTL_MS` (Prompt 14). **TODO_DECISION** si une autre durée est requise.
- **Détection hors-ordre par `provider_transaction_id`, pas par transaction Naminto.Ex** : un événement plus ancien que le dernier déjà `processed` pour le même `(provider, provider_transaction_id)` est rejeté, y compris lorsqu'aucune transaction n'a encore été corrélée — le hors-ordre est une propriété du flux d'événements fournisseur, indépendante de la corrélation applicative.
- **Rejeu contrôlé, distinct de la protection anti-rejeu** : un admin habilité (`webhook.manage`) peut rejouer un événement audité — nouvelle ligne (`replay_of`/`replayed_by`), l'originale n'est jamais modifiée. Le rejeu ignore volontairement la fenêtre de fraîcheur (une donnée historique en est par nature hors) mais revérifie idempotence et hors-ordre contre l'état actuel, jamais contre l'état figé au premier passage — un rejeu d'un événement déjà traité reste un no-op sûr (`duplicate`). Un événement à la signature invalide ne peut jamais être rejoué (rien de fiable à retraiter).
- **Aucune transition de statut de transaction ni écriture Ledger déclenchée par un webhook** : aucune source ne définit ce comportement asynchrone, et l'orchestrateur (Prompt 09) règle déjà chaque transaction de façon synchrone dans la même requête. Introduire une confirmation asynchrone par webhook aurait été une hypothèse métier non demandée (Master Prompt, section 10 — scope control). Un événement est seulement corrélé (best-effort, `provider_transaction_id`) à une transaction existante, pour l'audit uniquement. **TODO_DECISION** si un flux de confirmation asynchrone réel est requis plus tard.
- **Permission assignée à `operations`** (`webhook.read`/`webhook.manage`) — même rôle que `provider.read`/`provider.manage` (Prompt 23), cohérence directe avec le Provider Gateway. **TODO_DECISION** si un autre rôle doit également y avoir accès.
- **Statut** : Adopté.
- **Vérifié empiriquement** : 24 tests (signature HMAC pure ; `verifyAndParseWebhook` de l'adapter SANDBOX ; `processIncomingWebhook` bout en bout contre le vrai projet Supabase — accepté, signature absente/falsifiée, payload malformé, rejeu trop ancien, duplication, hors-ordre, corrélation à une vraie transaction ; `adminReplayWebhookEvent` — identifiant inconnu, signature invalide, no-op sûr, rejeu réussi, hors-ordre revérifié) ; `tsc`, `eslint` et `npm run build` propres ; vérifié dans le navigateur : liste `/admin/webhooks`, filtres, contenu brut, rejeu contrôlé.

## ADR-054 — Event Bus : notification découplée via un seul consumer réel, idempotence en deux couches (infra + consumer), backoff 30s/5 tentatives

- **Date** : 2026-08-28
- **Contexte** : le Prompt 26 exige de construire l'architecture événementielle autour de 12 événements nommés, avec des consumers idempotents et retry/dead-letter/correlation ID/event ID/timestamps/tracing. Aucun broker externe (Kafka/SQS/Pub-Sub) n'existe dans ce dépôt — comme pour Reconciliation (Prompt 24) et Webhooks (Prompt 25), l'infrastructure est réelle mais interne, persistée en Postgres, sans fausse intégration à un service externe qui n'existe pas.
- **Trois tables, une par exigence du prompt** : `domain_events` (event ID, correlation ID, timestamps) ; `event_deliveries`, `unique(event_id, consumer)` — la garantie d'idempotence *infrastructurelle* : un consumer ne peut jamais être enregistré deux fois pour le même événement ; `event_delivery_attempts`, une ligne par tentative réelle (exigence « tracing » traitée comme distincte de « retry », pas un simple compteur réutilisé).
- **Idempotence en deux couches, pas une seule** : la contrainte `unique(event_id, consumer)` empêche un DOUBLE ENREGISTREMENT de livraison, mais ne protège pas contre un *retry* d'une livraison déjà partiellement réussie (le consumer a agi, puis l'écriture du statut a échoué avant que la ligne soit marquée `succeeded`). Pour `notification-engine` (Prompt 20), dont `sendNotification` insère toujours une nouvelle ligne sans déduplication propre, le consumer ajoute donc sa propre vérification (`(event_type, reference)` déjà notifié ?) avant d'agir — exigence explicite du prompt (« chaque consumer doit être idempotent »), pas une garantie qui pouvait rester uniquement infrastructurelle.
- **Un seul consumer réel enregistré, `NotificationRequested`** : décision de découpler la notification de l'orchestrateur (elle n'appelle plus `notifyTransactionSettled`/`notifyTransactionFailed` directement, Prompt 20 — ces fonctions n'ont pas changé, seul leur appelant a changé) parce que cette relation avait déjà exactement la garantie qu'un event bus formalise (« une panne de notification ne doit jamais annuler une transaction confirmée »). Les 8 autres événements de cycle de vie transactionnel, `RiskDecisionMade` et `KYCStatusChanged` sont publiés pour de vrai (pas décoratifs) mais sans consumer pour l'instant — aucun besoin identifié ne le justifie encore. **TODO_DECISION** si un futur prompt requiert un consumer réel pour l'un d'eux.
- **`TransactionReversed`/`TransactionRefunded` publiés depuis `recordReversal`/`recordRefund`** (Ledger, Prompt 12) — ces fonctions existent et sont testées depuis le Prompt 12 mais ne sont déclenchées par aucun flux réel (aucun écran de litige/remboursement n'existe encore, TODO_DECISION déjà documenté) : l'événement est publié dès que la fonction l'est, pas une anticipation d'UI qui n'existe pas.
- **`publishEvent` ne lève jamais** — choix délibéré, plus strict que l'ancien pattern (chaque appelant encapsulait son propre try/catch autour de `notifyTransactionSettled`) : la garantie est désormais portée par la primitive elle-même, pas répétée à chaque site d'appel.
- **Aucune transition de statut de transaction déclenchée par un consumer** — cohérent avec la même décision pour les webhooks (ADR-053) : la seule voie de règlement reste l'orchestrateur synchrone (Prompt 09), pour ne pas introduire une source de vérité concurrente non demandée par ce prompt.
- **Backoff 30 secondes doublant à chaque tentative, 5 tentatives maximum avant dead-letter** — valeurs raisonnables non documentées dans les sources, même statut que `REPLAY_WINDOW_MS` (Prompt 25). Un rejeu manuel (`retryDeliveryNow`, Back Office) d'une livraison en dead-letter ne réinitialise jamais `attempts` : une seule tentative supplémentaire est accordée avant d'y retourner en cas de nouvel échec, jamais un budget reparti de zéro. **TODO_DECISION** si d'autres valeurs sont requises.
- **Permission assignée à `operations`** (`event.read`/`event.manage`) — même rôle que `provider.*`/`webhook.*` (infrastructure système). **TODO_DECISION** si un autre rôle doit également y avoir accès.
- **Statut** : Adopté.
- **Vérifié empiriquement** : 9 tests (publication + idempotence du consumer notification — deux publications successives pour la même transaction ne créent jamais deux notifications, contre le vrai projet Supabase ; retry avec backoff, dead-letter après 5 tentatives chacune tracée individuellement, rejeu manuel limité à une tentative supplémentaire) ; `tsc`, `eslint` et `npm run build` propres ; aucune régression sur les 28 tests orchestrateur/ledger/KYC déjà existants (le rewire de la notification ne les a pas affectés) ; vérifié dans le navigateur : `/admin/events`, filtres, lot manuel, rejeu d'une livraison.

## ADR-055 — Observability : six mesures dérivées de tables existantes plutôt que dupliquées, instrumentation à deux points d'entrée seulement, trace de transaction fusionnée

- **Date** : 2026-08-28
- **Contexte** : le Prompt 27 exige de mesurer neuf indicateurs (API latency, error rate, provider latency, provider errors, transaction success rate, webhook failures, reconciliation anomalies, notification failures, authentication anomalies), de corréler chaque requête importante à un request ID, et de pouvoir suivre chaque transaction de bout en bout. Aucune plateforme d'observabilité externe (Grafana/Datadog/Prometheus) n'est connectée dans ce dépôt — TODO_DECISION déjà documenté depuis le Prompt 01.
- **Six des neuf mesures dérivées, jamais dupliquées** : transaction success rate (`transactions`), webhook failures (`webhook_events`, Prompt 25), reconciliation anomalies (`reconciliation_anomalies`, Prompt 24), notification failures (`notification_deliveries`, Prompt 20) et authentication anomalies (`security_events`, Prompt 04) ont toutes leurs données déjà écrites par des prompts antérieurs. Décision : `adminObservabilityOverview` les agrège (en réutilisant directement `adminAnomalyCounts`/`adminWebhookEventCounts` quand la forme existante suffisait) plutôt que de recalculer une deuxième source de vérité — cohérent avec l'exigence Back Office « jamais une règle dupliquée » (Prompt 22).
- **Deux mesures réellement nouvelles, deux tables** (`request_logs`, `provider_call_logs`, migration `0019_observability.sql`) — aucune table existante ne trace la latence, seule donnée genuinely absente du dépôt jusqu'ici.
- **Instrumentation limitée à « chaque requête importante »**, interprété littéralement : seuls le Payment Orchestrator (`payment.orchestrator`, le chemin financier critique, Prompt 09) et la route webhook (Prompt 25) écrivent dans `request_logs`. Une page de navigation ou un rendu RSC normal n'est jamais journalisé — instrumenter chaque requête HTTP (assets, prefetch, navigation) aurait représenté un coût d'écriture disproportionné par rapport à l'esprit du prompt, qui qualifie explicitement les requêtes visées d'« importantes ». **TODO_DECISION** si une couverture plus large est requise.
- **Provider Gateway instrumenté à un seul endroit** (`providers/instrument.ts`, appliqué dans `registry.ts`) plutôt que dans chaque adapter — cohérent avec le principe déjà établi « ajouter un fournisseur ne nécessite qu'un adapter + son enregistrement » (Prompt 07). `verifyAndParseWebhook` n'est délibérément pas chronométré : ce n'est pas un appel réseau, et il a déjà son propre audit (`webhook_events`, Prompt 25) — le chronométrer aurait doublé une mesure déjà couverte.
- **Request ID généré par `src/proxy.ts`**, propagé via l'en-tête `x-request-id` à toute requête qui atteint l'application (y compris les webhooks, publics au sens authentification mais toujours passés par le middleware) — réutilisé s'il est déjà présent (compatibilité avec un proxy en amont), jamais régénéré à chaque étape du traitement.
- **Suivi de bout en bout par fusion, pas par une nouvelle table** : `adminTransactionTrace` combine `transaction_status_events` (Prompt 08) et `domain_events` filtré par `correlation_id` (Prompt 26) — ces deux sources traçaient déjà intégralement le cycle de vie d'une transaction ; ce prompt les rend consultables (`/admin/observability`), il ne réinvente pas le traçage.
- **Fenêtre glissante par défaut de 24 heures** pour les mesures dérivées de compteurs — valeur raisonnable non documentée dans les sources, même statut que `REPLAY_WINDOW_MS` (Prompt 25) et le backoff de l'Event Bus (Prompt 26).
- **Sous-ensemble de `SecurityEventType` considéré comme une anomalie d'authentification** (`login_failed`, `pin_verification_failed`, `pin_locked`, `new_device_login`) — choix assumé, aucune source ne définit ce qui constitue une « anomalie » par opposition à un événement de routine (`login_success`, `logout`, `device_added`…). **TODO_DECISION** si un ensemble différent est requis.
- **Permission `observability.read` assignée à `security`** (déjà propriétaire de `audit.read`) — lecture seule, aucune mutation dans ce domaine, donc pas de variante `.manage`.
- **Statut** : Adopté.
- **Vérifié empiriquement** : 11 tests (instrumentation du Provider Gateway — résultat inchangé, erreur relancée telle quelle, `verifyAndParseWebhook` jamais chronométré ; les neuf mesures et le suivi de transaction contre le vrai projet Supabase) ; `tsc`, `eslint` et `npm run build` propres ; vérifié dans le navigateur : `/admin/observability`, tableau de bord et recherche de trace sur une transaction réelle.

## ADR-056 — Security Audit (Prompt 28) : audit offensif sur les 15 catégories exigées, cycle DETECT→CLASSIFY→FIX→TEST→DOCUMENT par constat

- **Date** : 2026-08-28
- **Contexte** : le Prompt 28 exige un audit offensif sur 15 catégories (auth bypass, privilege escalation, IDOR, injection, XSS, CSRF, secrets, logs sensibles, brute force, replay, duplicate payment, race condition, insecure direct object reference, permissions insuffisantes, données personnelles exposées), avec l'exigence explicite « Ne te contente pas de signaler les problèmes. Corrige-les. » L'audit a été mené par quatre passes de revue indépendantes (auth/authz ; injection/XSS/CSRF/secrets/logs ; brute force/replay/duplicate payment/race condition ; données personnelles exposées) couvrant l'intégralité de `src/` et de `supabase/migrations/*.sql`.
- **Ce qui était déjà solide, confirmé sans changement** : XSS (aucun `dangerouslySetInnerHTML` hors rendu SVG de QR code, jamais de HTML utilisateur injecté) ; CSRF (Server Actions protégées nativement par Next.js, les deux seules Route Handlers authentifient par signature/token à usage unique, jamais par simple cookie) ; secrets (aucune valeur en clair dans le code suivi, `.env.local` exclu de git, `service_role` jamais importable côté client grâce à `server-only`) ; IDOR (chaque accès par id utilisateur-fourni vérifié contre le propriétaire réel, y compris le point le plus à risque — résolution de compte lié dans le routing du Payment Orchestrator) ; replay (QR codes, jetons de demande d'argent, confirmation/réinitialisation email — tous vérifiés côté serveur, jamais seulement côté client) ; attribution de rôle (`adminAssignRole`/`adminRevokeRole`, INSERT/DELETE atomiques déjà corrects).

### Constats corrigés (DETECT → CLASSIFY → FIX → TEST → DOCUMENT)

| # | Catégorie | Constat | Sévérité | Correctif | Test |
|---|---|---|---|---|---|
| 1 | Injection | `identity/admin-queries.ts` (recherche admin) construisait un filtre PostgREST `.or()` par interpolation brute du terme de recherche — une virgule/parenthèse/point pouvait injecter une clause supplémentaire, exécutée avec les privilèges service_role de cet écran. | Faible-moyenne | Échappement des caractères significatifs du mini-langage PostgREST (`,.()\`) avant interpolation. | `identity/admin-queries.test.ts` — un terme injecté ne renvoie plus l'ensemble de la table. |
| 2 | Logs sensibles | `notifications/channels/sms.ts` journalisait le numéro de téléphone complet et le corps intégral de chaque SMS simulé — aucun template actuel n'est sensible, mais rien n'empêchait une fuite le jour où un template OTP serait ajouté. | Faible | Numéro masqué (2 derniers chiffres), corps remplacé par sa longueur. | Vérifié par lecture directe (changement trivial, pas de comportement métier à isoler). |
| 3 | Auth bypass | `/admin/incidents` (stub `ComingSoonPage`) n'appelait `requirePermission` nulle part — tout admin muni d'un rôle quelconque pouvait l'ouvrir, seule page du Back Office à rompre l'invariant « chaque page sous `(protected)` vérifie une permission explicite ». | Faible | Nouvelle permission `incident.read` (rôle `operations`), `requirePermission` ajouté à la page et au nav-config. | Vérifié par lecture directe + `tsc`/`build` (aucune logique métier à tester, un stub reste un stub). |
| 4 | Privilege escalation | `getUserRoles` (bootstrap RBAC) faisait un `SELECT count()=0` puis un `INSERT` non atomiques — deux comptes distincts accédant à `/admin` au même instant sur un projet fraîchement migré pouvaient tous deux devenir `super_admin`, malgré un commentaire affirmant à tort que la contrainte unique suffisait à s'en prémunir. | Faible-moyenne | RPC `bootstrap_super_admin` (migration 0020) : `pg_advisory_xact_lock` sérialise tous les appels concurrents avant même la vérification, dans une seule transaction. | `rbac/queries.test.ts` — deux appels concurrents à la fonction elle-même échouent tous les deux une fois la table non vide. |
| 5 | Brute force | Verrouillage PIN (`verifyPinForUser`) : lecture de `failed_attempts` puis écriture de `+1` non atomiques — un burst de tentatives concurrentes pouvait toutes lire la même valeur non à jour et ne jamais déclencher le verrouillage après 5 échecs. | Moyenne | RPC `increment_pin_failed_attempts` (migration 0020) : `UPDATE ... SET failed_attempts = failed_attempts + 1` verrouille la ligne pour la durée de la transaction, sérialisant les tentatives concurrentes. | `identity/pin.test.ts` — un burst de 5 tentatives concurrentes atteint bien le verrouillage. |
| 6 | Brute force | Connexion (email + mot de passe) : `login_failed` déclaré dans `SecurityEventType` mais jamais journalisé, et aucune protection applicative au-delà des réglages par défaut de Supabase Auth. | Moyenne-haute | Nouveau domaine `login-lockout.ts` : verrouillage par hash SHA-256 de l'e-mail (jamais l'adresse en clair, jamais lié à `auth.users` — couvre aussi les tentatives contre un compte inexistant), RPC `record_login_failure` atomique (même mécanisme que le PIN). `security_events.user_id` étant `not null`, la journalisation par utilisateur réel restait impossible sans résolution email→id non fiable côté admin API — choix assumé de suivre par hash plutôt que d'inventer une résolution risquée. **TODO_DECISION** si une journalisation `security_events` par compte est requise en plus. | `identity/login-lockout.test.ts` — burst concurrent atteint le verrouillage, `resetLoginAttempts` le lève correctement. |
| 7 | Duplicate payment | `createTransaction` ne gérait pas la violation de contrainte unique (23505) sur `idempotency_key` en cas de course : le perdant recevait une erreur système au lieu de relire la transaction du gagnant — la contrainte empêchait déjà tout doublon réel, mais l'expérience était incorrecte pour un cas pourtant légitimement idempotent. | Faible (fiabilité, pas intégrité financière) | Capture du code 23505, relecture de la ligne existante — même correctif déjà appliqué à `getOrCreateLedgerAccount` (Prompt 12). | `payments/transactions.test.ts` — deux appels réellement concurrents avec la même clé renvoient la même transaction. |
| 8 | Duplicate payment | `recordSettlement`/`recordReversal`/`recordRefund` (Ledger) : vérification d'existence puis écriture, **sans aucune contrainte unique en base** pour rattraper une course (contrairement à `idempotency_key`) — deux appels concurrents pouvaient tous deux passer la vérification et produire un double lot d'écritures, un double débit/crédit réel. Constat le plus sévère de l'audit. | Haute | Nouvelle table `ledger_settlement_claims` (clé primaire `(transaction_id, kind)`, migration 0020) : réclamation atomique avant toute écriture — le perdant attend que le gagnant ait terminé (`waitForExistingEntries`) plutôt que d'écrire en double ou de renvoyer un lot vide. | `payments/ledger/record-entries.test.ts` — deux appels réellement concurrents ne produisent jamais deux lots. |
| 9 | Duplicate payment / Replay | `fulfillMoneyRequest` : `idempotencyKey` partagée par tous les payeurs potentiels d'un même jeton (choix délibéré à l'origine pour garantir un seul règlement), combinée à l'absence de réclamation avant l'appel à l'orchestrateur — deux payeurs concurrents pouvaient tous deux dépasser la vérification `status === "pending"`, et le second finissait par authentifier son propre PIN tout en faisant progresser une transaction dont le `sender_user_id` réel restait le premier payeur (mélange d'identité, pas seulement un doublon). Deuxième constat le plus sévère. | Haute | Réclamation atomique (`money_requests.claimed_by_user_id`, migration 0020) via une UPDATE conditionnelle AVANT tout appel à l'orchestrateur — le perdant est rejeté avant même de saisir son PIN. `idempotencyKey` désormais propre à chaque payeur (`money-request-${token}-${payerUserId}`, même schéma que le QR Engine, Prompt 15) : l'exclusivité « un seul règlement » est maintenant garantie par la réclamation, plus par la clé d'idempotence partagée. | `payments/money-requests/money-requests.test.ts` — deux payeurs concurrents : un seul gagne, la transaction réglée appartient bien au gagnant. |
| 10 | Permissions insuffisantes | `notifications_update_own` (RLS) autorisait la mise à jour de N'IMPORTE QUELLE colonne des notifications d'un titulaire (le commentaire du code affirmait « read_at uniquement, par convention applicative » — jamais appliqué au niveau base). Aucun chemin de ce dépôt n'exploite ce défaut, mais un appel direct à l'API Supabase avec le JWT du titulaire le pourrait. | Faible | `REVOKE UPDATE` puis `GRANT UPDATE (read_at)` sur `notifications` pour `authenticated` (migration 0020) — restriction au niveau colonne, la policy RLS existante restant inchangée (déjà correcte pour le périmètre de lignes). | Vérifié par lecture directe (changement de grant SQL, comportement applicatif inchangé — déjà couvert par les tests existants de `markNotificationRead`). |
| 11 | Données personnelles exposées | `payments/history/admin-queries.ts` (`select("*")`) envoyait la ligne `transactions` complète — y compris `destination_external_reference` (numéro de téléphone du destinataire externe), `source_reference`, `destination_reference`, `provider_transaction_id`, `idempotency_key` — au navigateur de tout admin muni de `transaction.read` (support, compliance, risk, finance, security), alors que les vues liste/détail n'affichent jamais que référence/montant/frais/statut/date. | Haute (numéro de téléphone d'un tiers, cinq rôles) | Colonnes explicitement sélectionnées (`AdminTransactionSummary`) au lieu de `select("*")`, dans la requête elle-même — jamais un filtrage a posteriori côté client. | `payments/history/admin-queries.test.ts` — la forme renvoyée n'a plus ces champs, même quand ils sont renseignés en base. |
| 12 | Données personnelles exposées | `identity/admin-queries.ts` (`adminGetUserDetail`) envoyait les lignes `devices`/`security_events` complètes — `device_fingerprint`, `ip_hash`, `metadata` brut (transitions KYC, raisons de blocage…) — à tout admin muni de `user.read` (support, kyc, compliance, security), alors que la fiche n'affiche que plateforme/statut et type/date. | Moyenne-haute | Colonnes explicitement sélectionnées (`AdminUserDeviceSummary`, `AdminUserSecurityEventSummary`). | `identity/admin-queries.test.ts` — devices/securityEvents n'ont plus ip_hash/device_fingerprint/metadata. |
| 13 | Données personnelles exposées | `notifications/admin-queries.ts` (`adminListNotifications`) envoyait `body` (montant et référence exacts de la transaction) alors que seul le titre est affiché, à tout admin muni de `notification.read`. | Faible-moyenne | Colonnes explicitement sélectionnées (notification et livraison). | `notifications/admin-queries.test.ts` — la forme renvoyée n'a plus `body`/`metadata`/`user_id`. |

### Constat détecté et classifié, non corrigé avec la même rigueur — documenté plutôt que masqué

**Race condition — Limit Engine (`payments/limit-engine/check-limits.ts` + `usage-queries.ts`)** : les vérifications de plafond journalier/mensuel/de fréquence lisent un cumul (`getAmountUsage`/`getFrequencyUsage`) puis comparent en mémoire, sans verrou ni contrainte en base — deux transferts réellement concurrents du même utilisateur pourraient en théorie tous deux lire le même cumul avant que l'un ou l'autre ne soit compté. Atténué en pratique par l'ordre du pipeline (la ligne `transactions` du transfert en cours est déjà créée, donc déjà visible, avant que `checkLimits` ne lise le cumul — ce qui rend la vérification plus conservatrice, pas plus permissive), mais pas structurellement éliminé.

Un correctif réellement sûr exigerait soit (a) porter l'intégralité de la décision (lecture des règles + agrégation + comparaison) dans une seule fonction Postgres verrouillée par utilisateur (`pg_advisory_xact_lock`), soit (b) une session Postgres dédiée maintenue sur toute la durée de l'orchestrateur — la connexion mutualisée de Supabase-js invalide un verrou consultatif tenu sur plusieurs appels `.rpc()`/`.from()` séparés (le verrou expire dès que la connexion du premier appel retourne au pool). Les deux options représentent une restructuration architecturale plus large que ce qui peut être appliquée en toute sécurité dans ce prompt sans risquer d'introduire un verrou silencieusement cassé — une fausse confiance étant pire qu'une absence de correctif documentée. **TODO_DECISION**, voir tableau ci-dessous.

- **Statut** : Adopté pour les 13 constats corrigés ; le constat Limit Engine reste ouvert, documenté explicitement plutôt que deviné ou masqué.
- **Vérifié empiriquement** : 14 nouveaux tests dédiés à ces correctifs (en plus des tests existants, tous toujours verts) ; `tsc`, `eslint` et `npm run build` propres ; suite complète exécutée contre le vrai projet Supabase.

## ADR-057 — Multi-Country / Multi-Currency (Prompt 29) : CountryProfile comme agrégat sur les tables de règles existantes, un seul pays réel seedé, verrou de devise unique retiré du seul point où il comptait

- **Date** : 2026-08-28
- **Contexte** : le Prompt 29 exige de faire sauter l'hypothèse implicite « FCFA uniquement » qui traversait tout le cœur financier, et d'exposer un CountryProfile administrable (devise, langues, fournisseurs, rails, notes de confidentialité, tarification, limites, KYC/AML, documents légaux) par pays.
- **Décision 1 — CountryProfile est un agrégat, pas une nouvelle source de vérité** : `pricing`/`limits`/`kycAml` (`src/domains/countries/profile.ts::getCountryProfile`) ne sont pas de nouvelles tables — ce sont des lectures filtrées par `country` sur `fee_rules`/`limit_rules`/`compliance_rules`, qui portaient déjà cette dimension depuis les Prompts 10/11/19. Seuls `languages`/`providers`/`rails`/`privacy_notes` sont de véritables nouvelles colonnes sur `countries` (migration 0021). `legal_documents` gagne une colonne `country` nullable — `NULL` = document générique, s'applique à tous les pays, comportement des lignes existantes préservé à l'identique.
- **Décision 2 — un seul pays réel seedé (Côte d'Ivoire / XOF), pas de pays inventé** : la migration insère `('CI', 'Côte d'Ivoire', 'XOF', {fr}, {orange,mtn,moov,wave,prepaid_card}, {mobile_money,card})` — rendant explicite ce qui était déjà implicite dans tout le produit (+225, Orange/MTN/Moov/Wave, XOF) plutôt que d'inventer un second pays sans aucune donnée source pour le justifier.
- **Décision 3 — le verrou de devise unique n'a été retiré qu'au seul endroit où il constituait une vraie règle métier** : `validateRequest` (`orchestrator-steps/validate.ts`), la porte de validation structurelle du Payment Orchestrator, contenait `const SUPPORTED_CURRENCIES = ["XOF"]` codé en dur — la seule vraie violation de « aucune partie du cœur financier ne doit supposer FCFA uniquement ». Corrigé en gardant `validateRequest` pure (aucun accès base, invariant du Prompt 09) : `supportedCurrencies` est désormais un paramètre, résolu par l'appelant (`orchestrator.ts`, via `listActiveCurrencies()`) qui a déjà accès à la base — jamais la fonction pure elle-même. Les nombreux autres `?? "XOF"` de repli (Send Money, création de demande d'argent, préremplissage QR, inscription utilisateur) sont **délibérément non touchés** : construire une véritable UX de sélection de pays serait du scope creep pour ce prompt — aucune UX de ce type n'existe encore, et `identity_profiles` n'a aucune colonne `country`. **TODO_DECISION**, voir tableau ci-dessous.
- **Décision 4 — taxonomie `rails` assumée, non documentée dans les sources** : `mobile_money`/`card` est un choix raisonnable d'implémentation, aucune source ne définit cette dimension.
- **Décision 5 — filtrage silencieux plutôt que rejet dans `adminCreateCountry`** : langues/fournisseurs invalides dans la saisie Back Office sont silencieusement écartés (comme le pattern déjà établi ailleurs dans ce module) plutôt que de rejeter toute la création — un choix d'ergonomie pour un écran interne, la donnée n'étant jamais exposée côté client.
- **Bug constaté et corrigé pendant la vérification** : la ligne `'CI'` existait déjà en base (créée au Prompt 22, code/name/currency/active seulement) — l'`insert ... on conflict (code) do nothing` de la migration n'a donc pas rempli `providers`/`rails` sur cette ligne préexistante (restés à leur défaut `{}`), repéré en vérification manuelle du Back Office. Corrigé par une mise à jour directe de la ligne réelle (`providers`/`rails` remplis) et par un changement de la migration en `on conflict (code) do update set languages =, providers =, rails =` pour que ce cas ne se reproduise pas si la migration est rejouée sur un projet ayant déjà tourné le Prompt 22.
- **Statut** : Adopté.
- **Vérifié empiriquement** : 8 nouveaux tests (`payments/orchestrator-steps/validate.test.ts` — régression XOF + extensibilité devise nouvellement seedée ; `countries/profile.test.ts` — agrégation réelle par pays, inclusion des documents légaux génériques, exclusion de ceux d'un autre pays ; `countries/admin-actions.test.ts` — parsing/déduplication/filtrage silencieux des nouveaux champs), suite complète (300 tests, 54 fichiers) toujours verte contre le vrai projet Supabase, `tsc`/`eslint`/`npm run build` propres, migration 0021 appliquée au vrai projet Supabase, vérification manuelle dans le navigateur (liste/formulaire/page détail `/admin/countries`, régression Send Money réelle en XOF bout en bout, référence `NEX-EEBEED50`).

## ADR-058 — Final Production Gate (Prompt 30) : verdict NON production-ready, un seul défaut réel corrigé (TIMEOUT jamais atteignable)

- **Date** : 2026-08-28
- **Contexte** : le Prompt 30 exige une revue complète des 12 domaines, 15 catégories d'audit et 12 scénarios explicites, la génération de `docs/PRODUCTION_READINESS.md`, et interdit formellement de déclarer le produit « production ready » si une condition critique est en `FAIL` — tout en interdisant explicitement de développer de nouvelles fonctionnalités.
- **Décision** : voir `docs/PRODUCTION_READINESS.md` pour le détail complet, domaine par domaine. Trois `FAIL` critiques retenus, empêchant toute déclaration de readiness : (1) `limit_rules` vide — aucun plafond réel configuré ; (2) aucun fournisseur `REAL` connecté — aucun mouvement d'argent réel possible ; (3) aucune stratégie Backup/DR définie à aucun des 29 prompts précédents. Aucun de ces trois points n'est un défaut de code : ce sont des dépendances business/produit ou des décisions jamais prises, honnêtement déclarées plutôt que masquées ou devinées.
- **Un seul défaut de code réel trouvé et corrigé** : `TIMEOUT` (`OrchestratorErrorCode`) existait depuis le Prompt 09 et son mapping vers `expired` existait déjà dans `orchestrator.ts`, mais aucun code n'appelait jamais `new OrchestratorError("TIMEOUT", ...)` — un appel fournisseur qui ne répondrait jamais aurait laissé l'utilisateur indéfiniment en attente. Corrigé par `withTimeout()` (course entre l'appel fournisseur et un délai de 30s, `execute-provider.ts`), avec `executeProviderTransfer` acceptant désormais un `timeoutMs` paramétrable pour rester testable sans attendre un vrai délai. Ce correctif ferme un vrai gap de robustesse (Master Prompt : scénario « timeout » explicitement exigé), ce n'est pas une nouvelle fonctionnalité — le code d'erreur et son traitement en aval existaient déjà, seul le déclenchement manquait.
- **Portée volontairement limitée** : aucun framework E2E (Playwright/Cypress) ni outil d'audit d'accessibilité automatisé (axe-core) n'a été installé pendant ce prompt — les deux constitueraient une nouvelle capacité d'infrastructure de test disproportionnée pour un prompt de clôture qui interdit explicitement le développement de nouvelles fonctionnalités. Documentés comme TODO_DECISION plutôt que construits dans l'urgence.
- **Vérifié empiriquement** : 304 tests (55 fichiers, +4 pour le correctif TIMEOUT), `tsc`/`eslint`/`npm run build` propres, suite complète toujours verte contre le vrai projet Supabase. Vérification manuelle : action admin non autorisée réellement bloquée (compte réel sans rôle → `/admin/forbidden`), revue responsive sans débordement (375px, trois écrans), flux Send Money réel bout en bout (référence `NEX-EEBEED50`).
- **Statut** : Adopté. Le produit reste au statut « projet de développement vérifié », pas « production ready » — voir `docs/PRODUCTION_READINESS.md` §10 pour l'ordre des actions à mener avant toute mise en production réelle.

## ADR-059 — Revue de code complète avant publication GitHub : 5 agents indépendants sur l'intégralité de src/, 21 défauts réels corrigés

- **Date** : 2026-08-28
- **Contexte** : avant de publier ce dépôt sur GitHub, l'utilisateur a demandé une vérification complète du code source — au-delà de l'audit ciblé du Prompt 28 et de l'audit de readiness du Prompt 30, une relecture indépendante cherchant spécifiquement des défauts jamais couverts par les tests existants.
- **Méthode** : cinq agents de revue indépendants, chacun scopé sur un sous-ensemble cohérent du cœur applicatif (orchestrateur/Ledger ; moteurs de règles Fee/Limit/Risk/Fraud/Compliance ; Identity/RBAC/sécurité ; Providers/Webhooks/Event Bus/QR ; Money Requests/History/admin Back Office), chacun lisant l'intégralité de ses fichiers assignés et cherchant activement des bugs de concurrence, des fuites de données, des incohérences d'idempotence et du code mort — pas une simple relecture stylistique. Chaque défaut rapporté a été revérifié manuellement (lecture directe du code) avant correction ; aucun correctif appliqué sur la seule foi d'un agent.
- **Défaut le plus sévère** : `runPaymentOrchestrator` (`orchestrator.ts`) renvoyait `replayed: true` — un succès — pour le rejeu d'une `idempotencyKey` dont la tentative précédente s'était en réalité terminée en échec (`!isInFlight(status)` est vrai aussi bien pour `settled` que pour `failed`/`rejected`/`expired`/`cancelled`). Exploitable directement via `fulfillMoneyRequest` (PIN corrigé après un premier `AUTH_ERROR`, même `idempotencyKey`) et `sendMoneyAction`. Corrigé par `isSuccessfulTerminalStatus` (`transaction-status.ts`), qui ne couvre que `settled`/`reversed`/`refunded`/`disputed` — tout le reste lève désormais une erreur explicite exigeant une nouvelle clé.
- **Trois autres défauts de concurrence réels**, chacun avec son propre test reproduisant la course par de vrais appels concurrents (`Promise.all`/`Promise.allSettled`, jamais simulés séquentiellement) :
  - `cancelMoneyRequest` (`cancel.ts`) pouvait annuler une demande entre la réclamation atomique et l'écriture finale de `fulfillMoneyRequest` (celle-ci n'étant conditionnée que par `claimed_by_user_id`, jamais par `status`) — corrigé en bloquant l'annulation dès qu'un payeur a réclamé la demande (`claimed_by_user_id is null` ajouté au filtre), pas seulement une fois le statut déjà changé.
  - `processIncomingWebhook` (`process-webhook.ts`) : vérification de duplication puis insertion non atomiques — deux livraisons vraiment concurrentes du même `event_id` pouvaient toutes deux devenir `processed`. Corrigé par un index unique **partiel** (`webhook_events_processed_original_unique_idx`, migration 0022 — seulement `status='processed' and replay_of is null`, pour ne jamais bloquer un rejeu Back Office légitime) et une capture du code 23505 traitant le perdant comme un duplicate.
  - `notification-consumer.ts` : idempotence vérifiée par `(event_type, reference)`, pas par destinataire — un règlement portefeuille-à-portefeuille notifie deux utilisateurs indépendants pour la même référence ; un retry après une interruption entre les deux envois sautait tout le traitement dès qu'UN SEUL avait déjà sa ligne, privant l'autre de notification de façon permanente et silencieuse. Corrigé par un `skipUserIds` par destinataire.
- **Un défaut métier réel dans le Limit Engine** (Prompt 11) : `getAmountUsage`/`getFrequencyUsage` agrégeaient l'usage tous fournisseurs confondus, même pour une règle spécialisée sur un fournisseur (`provider` non NULL) — un plafond `wave`-spécifique pouvait être déclenché par de l'usage réel sur `orange`. Corrigé en propageant la dimension `provider` de la règle effectivement retenue jusqu'à la requête d'usage.
- **Un défaut réel dans l'agrégation CountryProfile** (Prompt 29) : `getCountryProfile` ignorait les règles génériques (`country` NULL, joker) pour `pricing`/`limits`/`kycAml`, alors que `legalRules` les gérait déjà correctement — un pays reposant sur le seul repli XOF générique apparaissait à tort sans aucune tarification configurée.
- **Un défaut de sécurité réel** : `createPinAction` permettait de remplacer un PIN déjà défini sans prouver la connaissance de l'ancien — une session volée pouvait installer un nouveau PIN puis l'utiliser immédiatement pour autoriser un paiement. Corrigé en exigeant `verifyPinForUser` (même verrouillage anti-brute-force que `verifyPinAction`) avant tout remplacement ; champ « PIN actuel » ajouté au formulaire de changement (jamais à la création). Vérifié manuellement dans le navigateur : mauvais PIN actuel rejeté (« PIN incorrect »), bon PIN actuel accepté.
- **Une policy RLS trop permissive** : `devices_update_own` autorisait la mise à jour de n'importe quelle colonne, alors que le seul chemin client réel (`revokeDeviceAction`) ne touche que `status` — un JWT encore valide pour un appareil révoqué pouvait annuler sa propre révocation via un appel REST direct. Corrigé par `revoke update` + `grant update (status)`, même traitement que `notifications` (ADR-056).
- **Une fuite de données réelle** : `/pay/[token]` passait la ligne `money_requests` complète (`select("*")`) à `PayView`, un composant client — sérialisant `requester_user_id`/`claimed_by_user_id`/`fulfilled_transaction_id`/`id` dans le payload RSC envoyé à tout visiteur connecté ayant le lien, aucun de ces champs n'étant affiché. Corrigé par un nouveau type `PublicMoneyRequestView` (6 colonnes) construit côté page (Server Component), jamais la ligne complète transmise au client.
- **Corrections mineures additionnelles** : `fulfillMoneyRequest` — écriture finale de statut sans vérification d'erreur (journalisée désormais), réclamation atomique ne couvrant pas l'expiration (`.gt("expires_at", ...)` ajouté) ; `retryDelivery` (notifications) réinitialisait `attempts` à chaque relance au lieu de cumuler, permettant un nombre illimité d'envois réels sur plusieurs relances admin ; `pickMostSpecific` départageait les égalités de spécificité sur l'ordre physique des lignes en base (non garanti sans `ORDER BY`) — `.order("created_at")` ajouté aux trois moteurs de règles ; `adminSetCountryActive` renvoyait `{ok:true}` pour un id inexistant ; commentaire `TODO_DECISION` obsolète dans `proxy.ts` (RBAC déjà implémenté depuis le Prompt 23, jamais mis à jour) ; deux erreurs ESLint préexistantes jamais détectées faute d'avoir jamais lint tout le projet en une fois (`Date.now()` appelé pendant le rendu de deux Server Components, règle `react-hooks/purity`) ; une variable `t` inutilisée oubliée dans `country-profile-view.tsx` (Prompt 29).
- **Délibérément non modifiés** (jugements de conception, pas des bugs confirmés) : la règle FRAUD-002 (Fraud Engine) compte tout signal MEDIUM, y compris `amount`/`frequency` — périmètre plus large que sa description ne le suggère, mais aucun comportement incorrect démontré ; la spécificité d'une règle avec plage de montant (`min_amount`/`max_amount`) compte `+1` qu'elle soit bornée d'un ou des deux côtés — cohérent avec le reste du pattern « la plus spécifique gagne », pas une régression ; `withTimeout` (Prompt 30) gagne la course contre l'appel fournisseur mais ne l'annule pas réellement (aucun mécanisme d'annulation dans `ProviderAdapter`) — corriger cela ajouterait une vraie capacité d'annulation à l'interface, hors périmètre d'une revue de bugs ; la politique de mot de passe (8 caractères minimum) reste un TODO_DECISION déjà documenté, pas un nouveau constat.
- **Vérifié empiriquement** : 310 tests (56 fichiers, +9 par rapport au Prompt 30 — dont un bug résiduel dans le premier correctif de `cancel.ts` trouvé et corrigé par le test de concurrence lui-même avant d'être committé), `tsc`/`eslint`/`npm run build` propres, suite complète verte contre le vrai projet Supabase. Migration `0022_code_review_fixes.sql` appliquée à la vraie base. Vérifications manuelles dans le navigateur : changement de PIN (rejet/acceptation), aucune régression du flux Send Money.
- **Statut** : Adopté.

## ADR-060 — Tableau de bord : « réseau » de comptes (Naminto.Ex + comptes liés), solde de portefeuille enfin consultable

- **Date** : 2026-08-28
- **Contexte** : demande explicite de l'utilisateur, hors protocole des 30 prompts. Le tableau de bord (`/`) était resté un `ComingSoonPage` depuis le Prompt 03 ; le solde du portefeuille Naminto.Ex, bien que déjà calculable depuis le Ledger (Prompt 12), n'était encore consultable nulle part côté utilisateur (`docs/ARCHITECTURE.md` section 23, « Non couvert à ce stade »).
- **Décision** :
  - Titre de page supprimé — l'espace utile s'ouvre directement sur le contenu, cohérent avec la demande de ne garder que l'action.
  - Bouton « Lier un compte » sous forme de bouton rond « + » (`Button` existant, `size="sm"` + classes rond/carré via `className`, pattern déjà utilisé pour les icônes du header plutôt qu'une nouvelle variante `size="icon"` ajoutée au Design System) — déclenche exactement le même `LinkAccountDialog` que `/accounts` : le composant a été rendu réutilisable via une prop `trigger` optionnelle (élément déclencheur personnalisable, texte par défaut conservé sur `/accounts`) plutôt que dupliqué.
  - Nouvelle fonction `getWalletBalances` (`ledger/queries.ts`) : solde du portefeuille par devise, recalculé à la volée depuis `ledger_entries` (jamais stocké, même principe que le reçu ADR-044 et le Back Office Ledger `admin-queries.ts`, dont la pagination défensive au-delà de 1000 lignes est reprise à l'identique). Passe par le client RLS (`ledger_accounts_select_own_wallet`/`ledger_entries_select_own`, migration 0008) — la policy filtre déjà exactement ce qu'il faut, jamais de service_role pour une lecture du titulaire sur son propre portefeuille.
  - `getLinkedAccountsWithBalances` (`accounts/queries.ts`) factorise la boucle de résolution de solde SANDBOX précédemment dupliquée dans `/accounts/page.tsx` seul — désormais partagée par `/accounts` et le tableau de bord. `LinkedAccountCard` (nouveau composant) factorise de même le rendu de la carte, auparavant écrit en ligne dans `accounts-view.tsx`.
  - Correctif de contenu trouvé au passage : le texte d'avertissement du formulaire de liaison affirmait encore « Le Provider Gateway... n'est pas encore construit à cette étape du projet », alors qu'il l'est depuis le Prompt 07 (adapters SANDBOX réels, soldes réellement simulés — vérifié en liant un compte de test, solde SANDBOX 250 000 XOF affiché immédiatement). Reformulé pour refléter l'état réel (SANDBOX, jamais REAL) sans jamais prétendre à une connexion réelle.
- **Non testé par Vitest, vérifié manuellement dans le navigateur** : `getWalletBalances`/`getLinkedAccountsWithBalances` passent par le client RLS (`createClient()`, dépend de `cookies()`) — même limitation déjà documentée pour `listOwnMoneyRequests`/`getLinkedAccounts`, testable uniquement dans un vrai contexte de requête Next.js. Vérifié : solde réel affiché (compte démo, −1 425 XOF, cohérent avec l'historique de transactions de test de ce dépôt) ; liaison d'un compte Orange de test → apparaît immédiatement dans le réseau avec son solde SANDBOX ; déliaison → disparaît ; `/accounts` inchangé après factorisation ; FR/EN.
- **Statut** : Adopté.

## ADR-061 — Dépôt et retrait : les deux combinaisons source/destination manquantes n'exigeaient aucun changement de domaine, seulement une UI

- **Date** : 2026-08-28
- **Contexte** : demande explicite de l'utilisateur — vérifier que chaque réseau (Orange/MTN/Moov/Wave/carte prépayée) peut envoyer aussi bien en interne (« intra ») qu'à l'extérieur (« extra »), et combler ce qui manque. Depuis le Prompt 13 (ADR-041), Send Money n'exposait que deux combinaisons sur les quatre possibles : `naminto_wallet → naminto_wallet` (P2P) et `linked_account → external` (envoi externe). Les deux manquantes — `linked_account → naminto_wallet` (dépôt/recharge) et `naminto_wallet → linked_account` (retrait) — sont, dans les deux cas, un virement vers/depuis un compte du **même titulaire**, jamais un tiers.
- **Constat central** : en investiguant, ces deux combinaisons étaient déjà entièrement supportées par le domaine, sans aucune modification nécessaire :
  - `orchestrator-steps/routing.ts` résout déjà `destinationLinkedAccountId` (pas seulement `sourceLinkedAccountId`), avec une vérification de propriété (`account.user_id === senderUserId`) déjà correcte pour ce cas — elle exigeait déjà implicitement qu'un compte lié en position de destination appartienne à l'expéditeur, jamais à un tiers.
  - `execute-provider.ts` dérive déjà `direction` (`transfer` vs `receive`) uniquement de `sourceType`, ce qui produit automatiquement le bon sens pour les deux nouvelles combinaisons (`transfer` débite un compte lié en position source, `receive` crédite un compte lié en position destination).
  - `ledger/record-entries.ts::destinationAccountRef` mappe déjà `destination_type = 'linked_account'` vers `provider_suspense` (ADR-039, jamais modifié depuis).
  - `risk-engine/usage-queries.ts::resolveIsNewBeneficiary` traite déjà explicitement un compte lié en destination comme n'étant jamais un bénéficiaire (commentaire déjà présent : « Un compte lié du titulaire lui-même n'est jamais traité comme un bénéficiaire »).
  - `fee.ts`/`compliance.ts` sont déjà entièrement génériques sur `sourceType`/`destinationType`.
  Seule l'UI manquait — aucune règle métier n'a été ajoutée ni modifiée.
- **Décision** :
  - `SendMoneyRecipient` (`payments/actions.ts`) étendu de deux nouveaux modes (`deposit`, `withdraw`), `sendMoneyAction` construit la `PaymentRequest` correspondante — toujours `feePayer: "sender"` (un virement vers soi-même n'a pas de sens à faire payer les frais par « le bénéficiaire », qui est la même personne).
  - Nouveau `/transfer` (`direction=deposit|withdraw`, `account=<id>` optionnel prérempli et revérifié côté serveur contre les comptes actifs du titulaire — même principe que `source` sur `/send`) : wizard minimal (Montant → PIN → Récapitulatif → Résultat), sans étape « bénéficiaire » puisque la contrepartie est toujours implicitement soi-même. `StepShell`/`SummaryRow` extraits de `send-money-wizard.tsx` vers `send/step-shell.tsx` pour être partagés, jamais dupliqués.
  - Accès : bouton « Déposer » sur chaque carte de compte lié (tableau de bord et `/accounts`, via `LinkedAccountCard`) ; bouton « Retirer » sur la carte Naminto.Ex du tableau de bord (mène à `/transfer?direction=withdraw` sans compte prérempli — un pas « Choisir un compte » s'affiche alors).
- **Vérifié manuellement dans le navigateur, avec de vrais mouvements SANDBOX** (pas seulement des statuts de transaction) : dépôt 10 000 XOF Orange → portefeuille (portefeuille +10 000, solde SANDBOX Orange inchangé — même processus serveur redémarré entre-temps, en mémoire donc réinitialisé, sans lien avec le correctif) ; retrait 2 000 XOF portefeuille → Orange (portefeuille −2 070, Orange SANDBOX +2 000) ; **retrait croisé 1 500 XOF portefeuille → Wave (portefeuille −1 552,5, Wave SANDBOX +1 500, Orange inchangé)** — preuve d'un mouvement réellement inter-réseaux (Orange → portefeuille → Wave, deux réseaux distincts, via le portefeuille comme intermédiaire) ; carte prépayée testée pour les deux capacités (envoi externe déjà existant, et dépôt nouveau) ; P2P (`naminto_wallet → naminto_wallet`) revérifié fonctionnel après les changements de `actions.ts`.
- **Bug d'infrastructure rencontré et corrigé en cours de route, sans rapport avec la logique métier** : après une factorisation de `StepShell`/`SummaryRow` hors de `send-money-wizard.tsx`, le serveur de développement (Turbopack) a affiché une erreur « défini plusieurs fois » alors que le fichier sur disque ne contenait plus aucune définition locale (confirmé par grep direct et par `tsc`/`eslint` propres) — un artefact de cache HMR, pas un vrai bug de code. Résolu en arrêtant les processus du serveur, en supprimant `.next/`, et en redémarrant proprement.
- **Statut** : Adopté. Referme la ligne TODO_DECISION Payments correspondante (ADR-041) pour `linked_account → naminto_wallet`. `naminto_wallet → external` (un envoi direct du portefeuille vers un numéro externe sans passer par un compte lié précis) reste, lui, volontairement non exposé — un tel envoi n'aurait pas de rail fournisseur déterminé sans compte lié, resterait un TODO_DECISION si un besoin réel apparaît.

## TODO_DECISION en attente (issues des spécifications de domaine)

Ces points sont explicitement non définis dans les documents source. Ils ne doivent pas être devinés ; ils doivent être tranchés par l'utilisateur au moment où le prompt correspondant les rend bloquants.

| Domaine | Décision manquante |
|---|---|
| Identity | Fournisseur SMS pour activer le flux téléphone + OTP (ADR-014) ; biométrie/WebAuthn (non implémentée) ; authentification supplémentaire (step-up) explicite sur nouvel appareil — actuellement seulement journalisée (`new_device_login`), pas bloquante ; politique de complexité mot de passe (au-delà du minimum 8 caractères) ; durée de vie des sessions Supabase (config par défaut non modifiée) ; rate limiting applicatif additionnel au-delà de celui de Supabase Auth (partiellement fermé par le verrouillage anti-brute-force sur la connexion, ADR-056 — reste TODO_DECISION si une protection réseau supplémentaire, ex. CAPTCHA, est requise) ; **journalisation `security_events` par compte réel pour un login_failed** (suivi actuellement par hash d'e-mail uniquement, `login_attempts`, jamais lié à `auth.users` — `security_events.user_id` est `not null`, une résolution email→id côté admin fiable restait à inventer, ADR-056) ; **`identity_profiles.status` ne quitte jamais `pending_verification`** (aucun flux ne le fait jamais passer à `active` — repéré au Prompt 13 en construisant `findRecipientByNamintoId`, qui ne filtre donc volontairement pas par `status` sous peine de rendre Send Money inutilisable pour tous les comptes existants) |
| User | Processus de changement de numéro de téléphone ; politique de changement du nom légal post-KYC (les deux restent en lecture seule dans `/settings` pour l'instant) ; intégration d'un fournisseur KYC externe (statut actuellement toujours `unverified`, jamais mis à jour automatiquement) |
| Payments | Barème dégressif réel au-delà du taux de repli 3,5 %/1000 FCFA (la table `fee_rules` le permet, mais aucune règle par palier n'est encore saisie) ; **valeurs réelles des limites** (`limit_rules` est vide — table prête mais aucun plafond journalier/mensuel/par transaction/de fréquence n'est configuré, **bloquant avant toute mise en production réelle**) ; notion de `user_tier` (colonne prête, aucun palier utilisateur n'existe encore côté User) ; politique de reversal auto vs manuel ; **aucune UX de sélection de pays** (`identity_profiles` n'a aucune colonne `country`, le Payment Orchestrator n'accepte plus qu'une seule devise codée en dur — Prompt 29, ADR-057 — mais les nombreux `?? "XOF"` de repli dans Send Money/création de demande d'argent/préremplissage QR/inscription restent tous non touchés tant qu'un second CountryProfile réel n'existe pas) ; workflow de résolution d'un statut `disputed` (ADR-027) ; **`naminto_wallet → external`** (envoi direct du portefeuille vers un numéro externe sans compte lié précis — pas de rail fournisseur déterminé sans compte lié, ADR-041/ADR-061 ; `linked_account → naminto_wallet` et `naminto_wallet → linked_account` sont désormais couverts par Dépôt/Retrait, ADR-061) ; scan QR réel (caméra) pour identifier un bénéficiaire ou payer une demande (ADR-041/ADR-042/ADR-043) ; **durée d'expiration des demandes d'argent** (7 jours par défaut, non documentée — ADR-042) ; **distinction produit entre les types de QR `REQUEST` et `PAYMENT_REQUEST`** (traités comme des variantes du même objet faute d'élaboration dans les sources — ADR-043, `REQUEST` sans point de génération UI) ; procédure de rotation du secret de signature QR (ADR-043) ; **seuils réels du Risk Engine** (constantes de code choisies par implémentation, calibrées pour ne pas casser la suite de tests existante plutôt que contre une donnée métier réelle — ADR-045, TODO_DECISION si une configurabilité en base ou des valeurs métier documentées sont requises) ; **règles réelles du Fraud Engine** (les 4 règles de `rules.ts` sont un point de départ raisonnable, pas une politique anti-fraude métier validée — ADR-046) ; file d'attente de revue manuelle pour les transactions `MANUAL_REVIEW_REQUIRED` (aucun écran Back Office ne permet encore d'en disposer) ; second facteur d'authentification réel pour `STEP_UP` (biométrie/WebAuthn, TODO_DECISION Identity déjà documenté) ; **distinction réelle entre `KYC_STANDARD` et `KYC_ENHANCED`** (`identity_profiles.kyc_status` binaire, les deux paliers appliquent aujourd'hui exactement la même exigence — ADR-047) ; **jeu de règles de conformité réel** (`compliance_rules` ne contient que l'unique règle 200 000 XOF seedée — pas de règles par pays, par fournisseur, etc. — ADR-047) ; `settings.kyc.threshold` (UI) non reconnecté à `compliance_rules`, texte codé en dur pouvant dériver de la règle réelle (ADR-047) ; **race condition non structurellement corrigée sur le Limit Engine** (lecture du cumul puis comparaison sans verrou ni contrainte en base, atténuée par l'ordre du pipeline mais pas éliminée — un correctif sûr exige soit une fonction Postgres unique verrouillée par utilisateur, soit une session dédiée sur toute la durée de l'orchestrateur, hors périmètre sûr de ce prompt — ADR-056) |
| Notifications | Enregistrement de jetons d'appareil pour PUSH (aucun fournisseur FCM/APNs connecté, canal restera UNAVAILABLE — ADR-048) ; fournisseur SMS réel (aucune clé API en environnement, canal restera SANDBOX simulé — ADR-048) ; couverture événementielle au-delà de `transaction_settled`/`transaction_failed` (`money_request_created`, blocages Fraud/Compliance, nouvel appareil, etc. — actuellement seulement dans `security_events`, jamais notifiés — ADR-048) ; écran Back Office `/admin/notifications` (scaffoldé, toujours `ComingSoonPage` — Prompt 22) |
| Assist | Véritable compréhension du langage naturel / intégration LLM (le pattern-matching de `intent-detector.ts` est un point de départ raisonnable, pas une NLU — ADR-049) ; contenu FAQ plus riche que les six thèmes de guidage actuels (la table `faq_entries` administrable depuis le Back Office, Prompt 22, n'est pas encore consommée par Naminto Assist) ; réponse asynchrone d'un agent humain visible côté utilisateur quand un ticket change de statut (Back Office Support existe désormais, Prompt 22, mais rien ne notifie le titulaire — dépendrait du Notification Engine, Prompt 20, si câblé plus tard) |
| Back Office | État réel d'un fournisseur (santé toujours `operational` en SANDBOX, faute d'Availability Engine, Prompt 47) ; state machine KYC réelle (`adminUpdateKycStatus` n'exige qu'un changement, aucune transition validée — ADR-050, probablement lié à un futur fournisseur KYC externe) ; contenu FAQ/Legal jamais consommé ailleurs dans le produit (aucune page CGU/confidentialité publique) ; Countries désormais consommé côté cœur financier (devises supportées du Payment Orchestrator, Prompt 29, ADR-057) mais toujours aucun sélecteur pays côté produit — un seul CountryProfile réel existe (CI/XOF) ; **taxonomie `rails` (`mobile_money`/`card`) assumée, non documentée dans les sources** (ADR-057) ; Incidents reste `ComingSoonPage`, aucun moteur sous-jacent (Availability Engine, Prompt 47) |
| RBAC | Correspondance rôle → permissions non documentée dans les sources, choix assumé (ADR-051) ; `transaction.review`/`transaction.cancel` déclarées mais toujours jamais câblées à une action réelle (aucun workflow de révision/annulation manuelle de transaction n'existe encore dans le produit) ; aucune notification à un utilisateur retiré de tous ses rôles |
| Reconciliation | Découpage en cinq types d'anomalies et paires de vues comparées non documenté dans les sources, choix assumé (ADR-052) ; aucun ordonnanceur/cron, réconciliation immédiate + lot manuel seulement (ADR-052) ; aucune notification envoyée quand une anomalie est détectée (dépendrait du Notification Engine) ; le lot manuel ne couvre que les 200 transactions terminales les plus récentes, pas un balayage complet ; correspondance rôle → permissions (`finance` uniquement) non vérifiée par une source (ADR-052) |
| Ledger | Plan comptable des comptes de transit (un `provider_suspense` par fournisseur uniquement, pas encore par pays — ADR-039) ; déclencheur réel de `recordReversal`/`recordRefund` (aucun écran de litige/remboursement n'existe encore) ; consultation du solde de portefeuille par l'utilisateur (les écritures existent, aucune UI ne les agrège encore) |
| Audit | Durée de rétention des journaux par juridiction ; liste des actions à double validation ; plateforme de stockage |
| Observability | Plateforme d'observabilité externe retenue (Grafana/Datadog/Prometheus) ; seuils d'alerte et notification automatique en cas de dégradation ; objectifs RTO/RPO ; fenêtre glissante par défaut (24h) et sous-ensemble de SecurityEventType considéré comme anomalie d'authentification non documentés dans les sources (ADR-055) ; couverture de request_logs limitée à deux points d'entrée (payment.orchestrator, webhooks) — pas chaque requête HTTP (ADR-055) |
| Accounts / Providers | Adapters REAL pour Orange/MTN/Moov/Wave/cartes — identifiants API, contrats, endpoints à obtenir auprès de chaque fournisseur (**bloquant pour toute mise en production**, ADR-058), y compris leur propre schéma de signature webhook (ADR-053) ; détection automatique `connection_expired`/`provider_unavailable` (Availability Engine, Prompt 47) ; mode `UNAVAILABLE` déclaré dans `ProviderMode` mais jamais exercé par aucun adapter enregistré, `healthCheck()` jamais consulté par l'orchestrateur avant `transfer`/`receive` (Prompt 30, ADR-058) |
| Webhooks | Fenêtre anti-rejeu de 5 minutes non documentée dans les sources (ADR-053) ; aucune transition de transaction ni notification déclenchée par la réception d'un webhook (choix de périmètre, ADR-053) — dépendrait d'un flux de confirmation asynchrone réel si requis plus tard ; rôle assigné (`operations` uniquement) non vérifié par une source |
| Event Bus | Backoff (30 s, doublant) et nombre maximal de tentatives (5) non documentés dans les sources (ADR-054) ; seul `NotificationRequested` a un consumer réel — aucun besoin identifié pour les 8 autres événements à ce stade ; aucune transition de transaction déclenchée par un consumer (choix de périmètre, ADR-054) ; rôle assigné (`operations` uniquement) non vérifié par une source |
| Production Gate (Prompt 30) | **Backup/DR jamais défini** (fréquence de sauvegarde, RTO/RPO, procédure de restauration testée — aucun des 29 prompts précédents ne l'aborde, ADR-058, **bloquant avant toute mise en production**) ; **aucune valeur réelle dans `limit_rules`** (déjà signalé sous Payments, escaladé en FAIL du gate final, ADR-058) ; aucun framework E2E automatisé (Playwright/Cypress) — chaque prompt vérifié manuellement dans le navigateur contre le vrai Supabase, jamais rejoué en CI ; aucun outil d'audit d'accessibilité automatisé (axe-core ou équivalent) — fondations Radix/labels explicites non vérifiées systématiquement (contraste, ordre de tabulation, lecteurs d'écran réels) |
| Revue de code pré-GitHub (ADR-059) | Règle FRAUD-002 (Fraud Engine) : périmètre de déclenchement plus large que sa description ne le suggère (compte tout signal MEDIUM, y compris amount/frequency) — jugement de conception non tranché, pas un bug confirmé ; `withTimeout` (execute-provider.ts) gagne la course contre un appel fournisseur lent mais ne l'annule pas réellement (`ProviderAdapter` n'expose aucun mécanisme d'annulation/AbortController) — un appel lent-mais-finalement-réussi après expiration du délai pourrait en théorie produire un mouvement fournisseur jamais reflété au Ledger, non démontré exploitable avec les adapters SANDBOX actuels ; `devices.status = 'revoked'` reste un signal consommé par le Risk Engine, pas une invalidation réelle de session (aucune primitive Supabase Auth pour révoquer une session par appareil spécifique — révoquer *toutes* les sessions d'un utilisateur existe côté Admin API mais serait disproportionné pour un seul appareil) |

Chaque nouveau `TODO_DECISION` rencontré pendant l'implémentation doit être ajouté à ce tableau plutôt que deviné.
