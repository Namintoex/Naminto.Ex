/**
 * Chaque fichier ci-dessous s'enregistre lui-même à l'import (voir
 * registry.ts) — importer ce module pour son effet de bord suffit à
 * activer tous les consumers, sans jamais les importer individuellement
 * ailleurs dans l'application.
 */
import "./notification-consumer";
