// Stub pour les tests Vitest : le paquet "server-only" réel lève toujours
// une erreur hors du pipeline de build de Next.js (c'est son rôle en
// production — voir vitest.config.mts pour l'alias qui pointe ici).
export {};
